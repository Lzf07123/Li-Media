# Li&Media Brand

> 版本：V1.0 ｜ 日期：2026-09-05 ｜ 状态：项目内权威品牌方案
> `packages/li-design` 仅作为首次设计参考，不是运行时依赖。相关：[MASTER.md](MASTER.md) · `frontend/src/index.css` · `frontend/src/lib/brand.ts`

## 品牌意图

Li&Media 是照片和视频回忆库，不是通用媒体站。界面安静、清晰、少装饰，让真实照片、视频、拍摄时间和来源成为主角。

- **信任优先**：状态、文件信息和发布边界明确可读；不上传失败、缺失文件或未发布内容不会被伪装成正常内容。
- **淡色科技感**：使用海玻璃淡色体系，主色只做小面积强调。
- **以动衬静**：入场一次、环境极慢极淡；内容区不做持续抢注意力动画。
- **单一事实来源**：颜色、阴影、动效只在 `frontend/src/index.css`；站点与界面文案只在 `frontend/src/lib/brand.ts`。
- **无障碍优先**：正文 AA、控件可辨、焦点可见、触控目标至少 44x44px。

## 项目槽位（22 项）

| # | 槽位 | Li&Media 值 |
| --- | --- | --- |
| 1 | 项目显示名 | `Li&Media` |
| 2 | 技术标识 | `limedia` |
| 3 | 一句话定位 | 一次上传，随时回看照片与视频回忆 |
| 4 | 品牌承诺 | 每条回忆都有清晰状态、稳定展示和明确来源 |
| 5 | 人格比喻 | 一位安静的档案管理员 |
| 6 | 符号隐喻 | 直线=时间线；折线=回忆路径；方块=媒体文件；锁=私密边界；光点=被记住的瞬间 |
| 7 | 主色（浅） | `#25786D` / `#1F6359` / `#D9F4EE` / `#FFFFFF` |
| 8 | 主色（深） | `#7FD4C6` / `#A5E4D9` / `rgba(127,212,198,0.16)` / `#17332E` |
| 9 | 中性色（浅） | `#F6FBF9` / `#FFFFFF` / `#EEF6F3` / `#35423F` / `#64736C` / `#E1ECE8` |
| 10 | 中性色（深） | `#3A3F45` / `#434950` / `#4B5259` / `#F0F2F4` / `#B8C0C7` / `#545C64` |
| 11 | 语义色（浅/深） | 浅：success `#2A7C52`、warning `#9A5C05`、destructive `#C43737`；深：`#86D6AC`、`#EAD48E`、`#E8A49A`，带文字软底用 `*-soft-solid` / `*-soft-fg` |
| 12 | 焦点环 | 浅色 `#25786D`、深色 `#7FD4C6`，2px + 2px offset |
| 13 | 字体栈 | 系统 sans + 中文回退，不加载远程字体 |
| 14 | 标题字体 | 不额外引入，使用同一系统字体栈 |
| 15 | Logo / favicon | `brand-logo.webp`：透明底 512x512 WebP，最小 32px；`favicon.svg`：透明底，最小 16px；周围净空不小于图标边长 1/4 |
| 16 | 令牌前缀 | `--limedia-*` |
| 17 | 主题存储键 | `limedia-theme` |
| 18 | slogan / 备案 | slogan 由 `brand.ts` 承载；备案上线前留空，不写假占位号 |
| 19 | 氛围浓度 | 公开页与后台都使用 soft：极光 2 枚、科技层 soft，表格与表单区域保持可读 |
| 20 | 浏览器品牌位 | `/favicon.svg`、浅色 `#25786d`、深色 `#7fd4c6`、描述来自品牌定位、`limedia-theme` 首帧脚本 |
| 21 | 强调色板 | ice / aqua / lilac / sage / mint / sand，strong/soft 明暗双套 |
| 22 | 按钮与光效 | 半透明单色主按钮 + 细描边 + 扫光；光效可见但克制 |

## 内容与视觉边界

- 内容类型只做 `photo` 和 `video`；不做电影、剧集、动漫、纪录片、音乐 MV、评分或 TMDB 类元数据。
- 照片使用真实原图或服务端生成的真实缩略图；视频使用原生控件播放。
- 缺失缩略图时只显示低对比类型图标，不生成标题哈希抽象占位图。
- 卡片保留类型角标与自然比例封面；说明、技术状态和播放/下载控制在首页查看器内分层展示。
- 后端公开接口只返回 `published`；前端不长期保存网盘直链，远期网盘播放必须由服务端短期签发。

## 动效与禁用视觉

- 入场动效一次性出现；环境动效极慢、极淡，只动 `transform` / `opacity` / `background-position`。
- 所有动画都有对应 `@keyframes`，并尊重 `prefers-reduced-motion`；移动端隐藏科技光束与光点。
- 禁止霓虹渐变、AI 紫粉、粉色系、大面积重色、夸张玻璃拟态和标题哈希占位图。
- 液态玻璃只用于卡片、空态和返回顶部等局部表面；承载正文时保持 AA 对比度。
