# Li&Media 并发限制与视频解码策略 Checklist

> 更新于 2026-09-06。本清单替代已完成的单网关传输清单，作为下一阶段方案基线；当前只定版目标与验收项，不执行功能改造。
> 目标口径：后端按任务类型限制并发；播放解码继续交给浏览器；服务端视频只做单帧 WebP 缩略图，不做完整视频转码，也不持久化媒体本体。
> 边界约束：百度网盘仍只作远程索引与按需读取；直链、凭证和用户数据不入库、不进入前端存储；Nginx 不缓存短时直链或带凭证响应。

## 1. 后端任务边界

- [ ] 盘点后端可并发任务：百度扫描、直链解析 / 探测、单帧派生、服务端流回退、缓存清理和清理统计。
- [ ] 定义任务隔离边界：CPU / IO / 外部 API / 长连接流分开治理，不使用一个全局锁阻塞所有任务。
- [ ] 明确每个任务的生命周期、超时时间、重试策略和失败语义。
- [ ] 确认所有派生任务使用 `memory_id + derivative_version + size` 作为幂等键。
- [ ] 确认后台清理不会误删正在生成的临时文件，也不会修改百度网盘资源。

## 2. 并发上限与排队

- [ ] 为百度扫描任务设置全局并发上限，单副本阶段先固定为 1。
- [ ] 为直链解析 / 探测设置独立并发上限，初始建议 4～8，避免播放请求风暴冲击百度 API。
- [ ] 为 FFmpeg 单帧派生设置独立并发上限，初始建议 2～4，按容器 CPU 和真实耗时调整。
- [ ] 为服务端视频流回退设置按用户 1～2 个、全局 16～32 个的上限；仅作为直链失败兜底。
- [ ] 为每个任务类型设置队列长度；队列已满时返回明确的 202 / 429 状态和提示，不允许请求无限挂起。
- [ ] 对相同 `memory_id + derivative_version + size` 做请求合并，只允许一个 in-flight 任务，其他请求等待或复用结果。
- [ ] 为首屏可见缩略图设置更高优先级，避免查看器 1280px 任务阻塞首页 240px 任务。
- [ ] 为任务设置可观测指标：active jobs、queue depth、等待耗时、失败率、百度限流错误率和取消次数。
- [ ] 为扫描和派生任务补充取消语义；客户端断开或媒体切换后，未开始任务不应继续排队。

## 3. 服务端视频单帧缩略图

- [ ] 服务端视频缩略图只提取一帧，并保存为等比 WebP；不执行完整视频转码。
- [ ] 首帧读取使用远程流按需拉取，不把视频本体写入业务存储。
- [ ] 临时文件在生成成功或失败后立即删除，确保 `media/tmp` 不残留。
- [ ] 先生成 240px；480 / 768 / 1280px 只在首页、查看器或真实视口需要时按需生成。
- [ ] 派生失败返回明确失败状态，不回退百度方形缩略图，也不暴露远程原始地址。
- [ ] 若首帧为纯黑，允许配置一个极小的单帧偏移量，但仍只提取一帧，不做逐帧扫描。
- [ ] 保留 ETag、`If-None-Match`、304 和一年 immutable 缓存。
- [ ] 派生规则、质量或帧偏移变化时更新 `MEDIA_DERIVATIVE_VERSION`。
- [ ] 管理端确认清理继续覆盖派生缓存、临时文件和 Nginx 缓存，并保持网盘资源不变。

## 4. 用户侧解码边界

- [ ] 视频播放继续由浏览器原生 `<video>` 解码；服务端不转码、不代理完整播放流，除非作为失败兜底。
- [ ] 查看器触发播放后才请求百度短时直链；首页卡片不请求视频本体。
- [ ] 直链只保存在组件内存，临期、403、404 或播放失败时按现有逻辑刷新并回退。
- [ ] 同一时间只允许一个视频播放任务；切换媒体或关闭查看器时停止播放、清空 `src` 并释放解码资源。
- [ ] 暂不要求客户端为首页缩略图抽帧；首访用户不应先下载视频片段。
- [ ] 若未来评估客户端生成首帧，需先验证百度直链 CORS、`tainted canvas`、HEVC / MOV / MKV 支持差异和移动端限制。
- [ ] 若未来接受客户端候选缩略图，必须使用管理员会话、限流、尺寸 / MIME / 版本校验，且服务端仍保留 FFmpeg 单帧兜底。

## 5. 配置与运维

- [ ] 将并发上限、队列长度、单帧偏移和超时时间加入 `.env.example`，并为每个变量补充用途和安全说明。
- [ ] 为单副本使用进程内信号量；为多副本预留 Redis 分布式锁或任务队列迁移路径。
- [ ] 保持 Nginx 公开列表、派生图、直链和流回退限流；后端任务上限只做第二层保护。
- [ ] 确认 `httpx` 连接池、数据库连接池和 FFmpeg 子进程池不会超过任务上限。
- [ ] 在后台运维视图或接口中暴露任务指标；操作日志仍不得包含完整直链、Token、Cookie 或敏感参数。

## 6. 自动化验收

- [ ] 单元测试覆盖各任务类型的并发上限、队列拒绝、超时和重复任务合并。
- [ ] 集成测试确认同一视频同一尺寸并发请求只触发一次 FFmpeg 派生。
- [ ] 集成测试确认临时文件在成功、失败、超时和取消后均被清理。
- [ ] 集成测试确认百度扫描任务不会并发触发，失败恢复不重复删除索引。
- [ ] Playwright 确认首页只请求选中缩略图尺寸，播放前不请求视频本体。
- [ ] Playwright 确认播放失败后只执行一次直链刷新，再进入服务端回退。
- [ ] 使用小型真实视频验证单帧耗时、临时文件峰值和并发上限收益，并记录优化前后指标。

## 7. 质量门禁与合并

- [ ] 后端 pytest 全量通过。
- [ ] 前端 typecheck 与 production build 通过。
- [ ] 设计令牌、品牌文案来源检查通过。
- [ ] `docker compose config` 通过，且只有 Nginx 暴露宿主端口。
- [ ] Playwright 冒烟、传输链路和体验用例全部通过。
- [ ] `git diff --check` 通过；本地任务分支保留合并提交合回 `main`。
