# Li&Media Experience Checklist

> 更新于 2026-09-06。本清单替换已完成的 Remote-First 百度网盘媒体阶段清单，进入体验优化阶段。
> 已完成基线：Remote-Only 索引、百度短时直链、服务端转发回退、自然比例瀑布流、详情状态条、后台 OAuth 授权和 async-delete 同步。
> 目标口径：从“能浏览”升级为“流畅回看”，重点解决加载体积、瀑布流稳定性、详情交互、移动端体验、失败恢复和无障碍。
> 边界约束：继续只操作百度索引和临时预览链接，不保存媒体本体，不改网盘资源。

## 1. P0：媒体加载与瀑布流稳定性

- [x] 拆分缩略图尺寸：瀑布流使用低中分辨率缩略图，详情查看器使用高分辨率图，避免首页一次加载大量 1600px 图。
- [x] 为图片增加响应式 `srcset` / `sizes`，按视口和 DPR 选择合适版本。
- [x] 为瀑布流图片补充稳定占位尺寸，消除图片加载前后的布局跳动。
- [x] 首图或可视区图片优先加载，非可视图片延迟加载；必要时使用 `content-visibility` 控制渲染成本。
- [x] 为服务端缩略图补充可复用的缓存策略，减少重复代理请求。
- [x] 瀑布流列数、间距和圆角在 360/768/1280/1600 视口下做统一断点规范。
- [x] 瀑布流卡片 hover/focus 只做轻量反馈，不引起布局位移或滚动跳变。

## 2. P0：详情查看器体验

- [x] 照片查看器支持双指缩放、拖拽平移、双击聚焦和还原。
- [x] 支持键盘操作：`Esc` 关闭，方向键缩放或平移，焦点始终可见。
- [x] 移动端查看器支持自然手势，关闭按钮和重试按钮保持 44px 以上可点击区域。
- [x] 图片加载中显示稳定的低对比占位，不闪烁、不挤压状态条。
- [x] 图片加载失败时保留“重试”，并提供“使用服务端回退链路”的隐式恢复。
- [x] 视频播放补齐缓冲中、可播放、拖拽中三种状态，进度条拖动时不出现黑屏空白。
- [x] 百度直链临近过期时自动刷新；前端收到 403 后自动重新请求 `direct-url` 并回退服务端链路。
- [x] 查看器打开时锁定页面滚动，关闭后恢复原滚动位置。

## 3. P1：浏览效率与信息层级

- [x] 将“照片 / 视频”筛选升级为带结果计数的分段控件，减少页面跳转感。
- [x] 搜索输入增加防抖，保留 URL 状态，支持清空后回到稳定默认视图。
- [x] 分页结果总数、当前筛选条件和空态文案统一展示在筛选区附近。
- [x] 返回首页时恢复上一次筛选、页码和滚动位置。
- [x] 瀑布流支持按拍摄时间或更新时间排序，排序状态写入 URL。
- [x] 移动端筛选区收起为单行横向滑动，不占用首屏过多高度。
- [x] 长列表保持分页或虚拟化，避免一次渲染过多大图。

## 4. P1：状态、错误与后台同步体验

- [x] 首页、详情和管理页的加载骨架使用同一节奏与令牌，避免视觉跳跃。
- [x] 详情状态条可折叠为轻量徽标，默认突出“可用 / 失败 / 重试”，弱化技术名词。
- [x] 直链失败、缩略图失败、视频失败统一展示恢复动作，而不是只显示技术错误。
- [x] async-delete 后台同步展示队列状态、处理进度、删除数量和完成时间。
- [x] 同步失败时保留断点续扫入口，并明确“只清理本地索引，不改网盘资源”。
- [x] 管理页操作成功 / 失败反馈保持一致位置和时长，不打断浏览焦点。

## 5. P1：移动端与无障碍

- [x] 移动端详情采用沉浸式媒体视图，状态条可收起。
- [x] 所有可交互目标保持至少 44×44px，横向滑动区域提供无障碍名称。
- [x] 弹窗、查看器和分页组件完整支持键盘循环焦点。
- [x] 文本对比度在明暗主题下均达到 AA。
- [x] 动效尊重 `prefers-reduced-motion`，关闭入场动画、缩放弹性和骨架闪烁。
- [x] 屏幕阅读器可理解媒体加载中、加载成功、加载失败和重试结果。

## 6. P2：微交互与品牌质感

- [x] 瀑布流图片使用渐进显影，避免生硬闪现。
- [x] 查看器开合使用统一缩放原点，动画柔和且 GPU 加速。
- [x] 视频缩略图增加轻量播放标识，hover 时只提升清晰度或透明度，不布局位移。
- [x] 明暗主题切换时媒体背景、边框和阴影保持连续过渡。
- [x] 空态、404 和加载失败保持同一插画语言与按钮层级。

## 7. 验收与质量门禁

- [x] Playwright 覆盖瀑布流点击、筛选恢复、查看器手势回退、直链 403 恢复和视频失败恢复。
- [x] 对 360/768/1280/1600 四档截图检查无横向溢出、无文本换行溢出、无卡片布局位移。
- [x] 明暗主题下检查图片背景、状态条、空态和错误态对比度。
- [x] 用 24 张媒体页面测量首屏加载、图片总传输体积和交互后布局位移。
- [x] 一键质量门禁继续覆盖后端测试、前端 typecheck/build、设计令牌与品牌来源检查、Playwright 冒烟。
- [x] 每个体验项完成后在浏览器中实测，而不是只依赖单测通过。
