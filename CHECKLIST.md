# Li&Media Viewer-First Transfer & Maintenance Checklist

> 更新于 2026-09-06。本清单是设计方案与实施验收清单，当前只完成方案替换，不执行功能改造。
> 目标口径：取消独立详情页，首页点击缩略图直接展开大图；照片与视频使用不同传输策略。照片预览走服务端派生缓存：首页 480px、查看器 1280px；视频海报同样分级派生，但视频本体播放/下载仍使用百度网盘短时直链。后台支持一键清理本地索引与缩略图缓存。
> 边界约束：继续只索引百度网盘资源，不保存媒体本体；清理只作用于本地数据库索引和本地派生缓存，绝不能修改或删除网盘资源；直链、凭证和用户数据不入库、不持久化到前端存储。

## 1. Viewer-First 路由与交互

- [ ] 移除首页到 `/memories/:memoryId` 的独立详情页入口，点击瀑布流缩略图直接打开媒体查看器。
- [ ] 查看器打开状态写入 URL，例如 `/?viewer=<memoryId>`，刷新、前进、后退、分享链接后仍能直接打开对应媒体。
- [ ] 旧地址 `/memories/:memoryId` 保留兼容重定向：跳转到首页并自动打开查看器，不再渲染详情页。
- [ ] 查看器支持照片放大：双指缩放、拖拽平移、双击聚焦/还原、键盘 `+/-/0/Arrow` 操作。
- [ ] 查看器支持视频播放、缓冲、拖拽进度、播放失败重试；照片与视频共用关闭、焦点和恢复逻辑。
- [ ] `Esc` 关闭查看器；弹层内焦点循环；关闭后恢复首页滚动位置和筛选状态。
- [ ] 查看器顶部或底部保留轻量状态摘要：可用、失败、重试；展开详情时不使用独立状态页。
- [ ] 关闭按钮、重试按钮、下载按钮保持至少 44×44px 可点击区域。

## 2. 缩略图分级传输

- [ ] 首页瀑布流缩略图统一请求 480px 级 WebP，不再让浏览器命中 1280px 缓存。
- [ ] 后端按请求尺寸生成真实 480px 变体，而不是复用 1280px 文件只修改 ETag。
- [ ] 缩略图缓存文件按媒体 ID、派生版本、请求尺寸隔离，避免 480px 和 1280px 互相覆盖。
- [ ] 缩略图保持原始宽高比，禁止百度方形裁剪结果直接作为瀑布流展示源。
- [ ] 480px 响应设置稳定 ETag 和长缓存策略；派生逻辑或裁剪规则变化时更新缓存版本。
- [ ] 内存卡片继续提供真实宽高或自然宽高比占位，加载前后不发生布局跳动。
- [ ] 首屏第一张缩略图可优先加载，其余按视口懒加载；非可视区域不提前消耗 1280px 传输。

## 3. 展开媒体与分类型传输

- [ ] 点击缩略图时先用已加载的 480px 图作为占位，再请求 1280px 大图。
- [ ] 1280px 图只在查看器打开后请求；关闭查看器后停止非必要预取。
- [ ] 1280px 大图加载中显示稳定占位，不挤压状态条、关闭按钮或下载按钮。
- [ ] 大图加载失败保留重试，并在必要时隐式回退服务端转发链路。
- [ ] 大图加载成功后更新屏幕阅读器状态；失败状态可被辅助技术理解。

### 照片传输策略

- [ ] 首页缩略图使用服务端派生的 480px WebP，不请求照片原图。
- [ ] 查看器内预览使用服务端派生的 1280px WebP；该响应可使用 ETag 与长缓存。
- [ ] 照片原图只在用户主动下载时按需获取；预览链路不得为了展示而隐式下载原图。
- [ ] 派生图按媒体 ID、派生版本和尺寸隔离缓存，避免 480px、1280px 与原始图互相覆盖。

### 视频传输策略

- [ ] 首页视频缩略图使用服务端派生的 480px 海报帧，不请求视频本体。
- [ ] 视频查看器可先展示 1280px 海报帧；该请求只派生图片，不代理视频本体。
- [ ] 用户触发播放时，视频本体优先请求百度网盘短时直链；服务端按需转发只作为直链失败后的回退。
- [ ] 百度短链仅在组件内存中保存，临期、403 或播放失败时重新请求 `direct-url`；不得写入浏览器存储或 URL。
- [ ] 视频不使用 `preload="auto"` 预加载；关闭查看器或切换媒体时必须停止播放、清空 `src` 并释放解码资源。
- [ ] 同一时间只允许一个视频直链播放任务，避免多个大文件并发消耗带宽。

## 4. 下载按钮与百度短链

- [ ] 查看器新增下载按钮，仅对已发布且可访问的照片或视频显示。
- [ ] 点击下载时向服务端请求 `direct-url`；服务端解析并返回百度网盘短时直链。
- [ ] 照片下载获取原始文件短链；视频下载复用播放短链或重新请求一条短链，服务端不代理视频本体。
- [ ] 前端只把短链保存在组件内存中，用于触发当次下载；禁止写入 localStorage、sessionStorage、Redux 持久层或 URL。
- [ ] 下载响应设置 `no-store` 类策略，代理路径与日志不得暴露完整直链或凭证。
- [ ] 短链临近过期、403 或下载失败时重新请求一次 `direct-url`；仍失败则展示统一恢复动作。
- [ ] 下载动作记录必要的后台操作日志，但不记录完整直链、Token 或敏感参数。

## 5. 后台一键清理

- [ ] 新增管理员专用清理接口，支持一次性清理全部本地媒体索引与本地缩略图缓存。
- [ ] 清理接口必须在确认后执行，请求体包含显式确认字段，避免误触发。
- [ ] 执行前返回 dry-run 统计：将删除的回忆数量、远程文件索引数量、扫描任务数量、缩略图文件数量和预计释放空间。
- [ ] 数据库删除在事务中完成：清理 `Memory`、`MemoryFile`、远程索引派生状态和过期的扫描任务数据；失败时整体回滚。
- [ ] 数据库提交成功后再清理 `media/thumbnails` 与 `media/tmp`，避免数据库回滚但文件已被误删。
- [ ] 清理操作不得访问、修改或删除百度网盘原始资源；操作日志必须明确 `remote_resource=untouched`。
- [ ] 保留管理员账号、会话、操作审计、OAuth 服务端凭证配置；不得通过清理接口泄露或删除 Token。
- [ ] 清理完成后自动失效进程内百度直链缓存，并返回清理数量、耗时和完成时间。
- [ ] 后台管理页新增清理入口、危险确认弹窗、执行中状态、成功/失败反馈和清理结果摘要。

## 6. 传输链路体验优化

- [ ] 查看器优先复用首页列表中已有的媒体元数据，减少打开时的重复详情请求。
- [ ] 旧链接或刷新进入查看器时才按 ID 获取单条媒体数据。
- [ ] 照片与视频海报的 480px / 1280px 派生图分别支持 `ETag`、`If-None-Match` 和 `304 Not Modified`。
- [ ] 响应缓存策略区分公开派生图和短时直链：派生图可长缓存，直链必须 `no-store`。
- [ ] 前端限制同时请求的大图数量，避免快速连续点击造成并发下载风暴。
- [ ] 图片派生请求和视频直链播放使用独立并发上限；视频本体不参与首页或海报预取。
- [ ] 媒体地址版本随派生规则更新，避免旧方形图、旧 1280px 图继续命中浏览器缓存。
- [ ] 后台清理后，前端旧缓存地址不应继续展示已删除索引。

## 7. 后端测试与验收

- [ ] 覆盖照片与视频海报的 480px 缩略图生成、真实宽高比、独立缓存路径、ETag 和 304。
- [ ] 覆盖照片查看器 1280px 派生图生成与缓存；确认首页不会隐式请求 1280px。
- [ ] 覆盖视频本体优先使用百度短链、直链过期刷新、403 处理和服务端回退边界。
- [ ] 覆盖 `direct-url` 短链请求、过期刷新、403 处理和日志脱敏。
- [ ] 覆盖一键清理 dry-run、确认执行、事务回滚、文件清理和网盘资源不变。
- [ ] 覆盖清理后首页、详情旧链接、下载按钮和后台列表的错误/空态表现。

## 8. 前端测试与真实浏览器验收

- [ ] Playwright 覆盖：点击缩略图打开查看器、旧详情地址重定向、关闭后恢复筛选和滚动。
- [ ] Playwright 覆盖：照片首页只请求 480px 派生图，查看器打开后才请求 1280px 派生图。
- [ ] Playwright 覆盖：视频首页只请求 480px 海报，播放时请求百度短链；直链过期或 403 时刷新后恢复。
- [ ] Playwright 覆盖：下载按钮请求短链并触发下载；直链失败时展示可理解的恢复动作。
- [ ] Playwright 覆盖：后台清理确认、执行反馈、清理后首页空态。
- [ ] 对 360 / 768 / 1280 / 1600 视口截图检查无横向溢出、无文本溢出、无布局位移。
- [ ] 明暗主题下检查查看器、占位、状态、错误态和下载按钮对比度达到 AA。
- [ ] 手动浏览器实测：首次打开、缓存命中、旧缓存失效、删除索引后访问、网盘资源未变化。

## 9. 一键质量门禁

- [ ] 后端 pytest 全量通过。
- [ ] 前端 typecheck 与 production build 通过。
- [ ] 设计令牌、品牌文案来源检查通过。
- [ ] Playwright 冒烟与新增体验用例通过。
- [ ] `git diff --check` 通过；本地任务分支保留合并提交合回 `main`。
