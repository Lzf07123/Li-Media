# Li&Media Next Checklist

> 更新于 2026-09-05。这份清单替换已完成的本地同步、后台安全和批量整理清单，进入 Remote-First 百度网盘媒体阶段。
> 目标口径：百度网盘只作为存储源；Li&Media 持久保存远程索引，不保存媒体本体；预览和播放按需短期解析。
> 设计系统、服务端会话鉴权、一键质量门禁、批量整理和本地上传链路已完成并合入 `main`。

## 1. Remote-First 索引扫描

- [x] 将百度网盘 Worker 改为 `scan-only` 模式，扫描和入库时不再下载媒体本体。
- [x] 递归扫描 `BAIDU_SYNC_DIR`，支持目录、文件、分页和深度限制。
- [x] 新增扫描任务与远程条目索引，记录 `fs_id`、路径、目录、文件名、大小、mtime、扩展名和原始元数据摘要。
- [x] 按媒体类型识别照片和视频，跳过目录、文档和其他非媒体资源。
- [x] 以 `fs_id + remote_path` 为主、Baidu `md5` 为辅做去重。
- [x] 增加增量扫描：同一远程文件未变化时只刷新扫描时间，不重建 `Memory`。
- [x] 记录扫描游标或检查点，任务可恢复，避免中途失败后从零开始。
- [x] 增加请求间隔、限流退避和单次扫描上限，避免触发百度接口限制。

## 2. 远程元数据与预览

- [x] 调用 Baidu 文件元数据接口，补充远程大小、修改时间、MD5、分类和可用预览图信息。
- [x] 从文件名、目录或远程元数据回填标题和拍摄时间，不依赖完整下载后的 EXIF。
- [x] 远程缩略图由服务端代理给前端，前端不接触百度直链。
- [x] 缩略图缺失时继续使用低对比图标占位，不回退下载原图。
- [x] 照片和视频详情展示远程状态、远程路径、大小、修改时间和可用预览状态。
- [x] 已发布内容才能访问远程元数据代理接口。

## 3. 按需播放与短期直链

- [x] 新增按需播放接口，由服务端短期解析百度下载地址并转发媒体流。
- [x] 支持视频 `Range` 请求，保证进度条拖动不要求把完整视频下载到本地。
- [x] 直链只保存在内存或短 TTL Redis 缓存中，不写入数据库、日志或前端存储。
- [x] 短期缓存按 `fs_id` 或远程 ID 复用，过期后重新解析。
- [x] 前端使用 Li&Media 自有播放地址；百度直链只出现在服务端请求和响应转发链路中。
- [x] 播放失败、远程缺失、Token 过期和接口限流分别给出可理解的错误状态。

## 4. 同步状态与管理页

- [x] 增加 `remote_ready`、`remote_missing`、`remote_failed`、`thumbnail_ready`、`stream_ready` 等可观测状态。
- [x] 管理列表展示扫描时间、远程路径、远程状态、预览状态和播放状态。
- [x] 管理页支持触发扫描、查看扫描进度、查看失败原因和重试单条远程条目。
- [x] 远程文件消失时标记缺失，不自动删除 `Memory` 记录。
- [x] 扫描、刮削、代理失败和重试动作写入管理操作日志。
- [x] 管理凭证和百度访问凭证只保留在服务端环境变量或部署配置中。

## 5. 测试与质量门禁

- [x] 使用 fake Baidu client 覆盖递归扫描、分页、重复文件、增量更新、失败重试和非媒体跳过。
- [x] 集成测试确认扫描模式不写入媒体本体，也不保留长期百度直链。
- [x] 覆盖远程缩略图代理和按需流式播放接口，包括缺失、未发布和远程失效场景。
- [x] 前端测试覆盖远程状态占位、详情加载失败和视频播放失败提示。
- [x] 一键验证脚本继续覆盖后端测试、前端 typecheck/build、Playwright 冒烟、设计令牌和品牌来源检查。
