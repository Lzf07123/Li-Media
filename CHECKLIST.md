# Li&Media 后台媒体盘点、视频兼容筛选与首页呈现 Checklist

> 更新于 2026-09-07（实施完成）。本清单取代“自适应媒体体验 Checklist”。
> 本轮已实施 C1–C7，并通过自动化门禁与真实容器验收；验收实测见 Ledger。
> 目标是让管理员看清媒体规模、按筛选范围批量发布或下线、保证筛选与计数一致；公开媒体自动筛除浏览器不可播放的视频；首页保持页脚可达、显示公开收录总量，并增强资源加载表现，例如卡片掠光。

## 0. 范围与边界

- [x] 只处理 `photo` 与 `video`，不新增内容类型。
- [x] 后台部分只调整管理接口、后台页面和必要的数据查询；不改 Source Zero、网盘扫描、派生生成和播放链路。
- [x] 首页部分只增强加载中的视觉反馈与状态切换、页脚常驻位置和公开收录总量展示，不改后端并发上限、派生算法、视频转码或播放传输链路。
- [x] 首页页脚采用固定或粘性常驻位置；无限画布再长，也不允许页脚变成必须滚动到底才能发现的入口。
- [x] 首页展示的“已收录媒体”是公开可浏览口径，不得通过显示数量泄露草稿、下线、排除或不可播放资源的存在。
- [x] 发布/下线只修改管理状态，不修改或删除百度网盘资源。
- [x] 公开展示仍受展示健康规则约束：`published` 不等于一定 `displayable`。
- [x] 公开视频展示必须叠加浏览器兼容筛选：已发布但浏览器不可播放的视频不进入公开画布，也不进入公开推荐。
- [x] 视频兼容自动筛选只调整展示状态和公开可见性，不删除索引、不修改百度网盘资源、不做完整转码。
- [x] 所有计数以后端查询为唯一权威；前端不得用当前页 `items.length` 推导总数。
- [x] 批量操作必须有明确范围、确认入口、结果反馈、审计记录和失败恢复语义。
- [x] 卡片掠光是加载状态反馈，不是常驻装饰；动画必须可禁用、可回收，且不影响真实媒体可见性。

## 1. 输入基线

以下数据来自 2026-09-07 真实容器，只作为测试参照，不是完成证明：

| 口径 | 数量 |
| --- | ---: |
| 后台索引总数 | 65 |
| 后台照片 / 视频 | 48 / 17 |
| 展示健康可展示 / 排除 | 64 / 1 |
| 公开列表可展示总数 | 64 |
| 公开列表照片 / 视频 | 47 / 17 |

> 验收实测：后台索引 65 / 48 / 17，展示健康 64 / 1；启用浏览器兼容筛选后，公开可浏览为 47 / 47 / 0，17 条视频全部被保守判定为 `unsupported`。扫描仅刷新远程索引，Source Zero 合规，临时文件为 0。

## 2. 后台媒体总数与分类计数

### 需求

- [x] 后台顶部或列表工具栏显示索引总数、照片数和视频数。
- [x] 计数必须覆盖后台索引全量，不受当前页大小或分页截断影响。
- [x] 类型计数显示为 `总数 / 照片 / 视频`，并满足 `照片数 + 视频数 = 总数`。
- [x] 状态筛选、展示健康筛选和关键词筛选生效时，结果数必须随之变化，但类型计数应能清楚区分“全局索引计数”与“当前筛选命中数”。
- [x] 若同时展示 `published`、`draft`、`displayable`、`excluded` 计数，标签必须写明口径，不能把发布状态与展示健康混为一谈。
- [x] 空库、零视频、零照片、接口失败和加载中分别有明确状态，不显示误导性的 0。

### 验收

- [x] 后端集成测试覆盖照片、视频、published、unpublished、displayable、excluded 的组合计数。
- [x] 任意筛选组合下，接口返回的 `total`、类型计数、状态计数与 SQL 查询结果一致。
- [x] 前端真实容器测试对比 API 响应与页面文本，确认计数一致。
- [x] 修改分页大小、翻页、切换筛选后，全局类型计数保持稳定。

## 3. 一键发布与一键下线

### 需求

- [x] 后台提供“一键发布当前筛选”和“一键下线当前筛选”两个明确操作。
- [x] 默认作用域是当前筛选命中的全部记录，而不是当前页。
- [x] 操作前弹出确认框，展示筛选条件、影响总数、照片/视频分布、发布/下线状态分布和展示健康分布。
- [x] “全部媒体”属于高风险作用域；若没有筛选条件，确认文案必须显式提示将影响全库。
- [x] 后端按同一筛选条件执行，不依赖前端上传全量 ID；超过 100 条时使用后台任务或分批处理。
- [x] 批量任务必须显示处理进度、成功数、跳过数、失败数和可重试原因；支持取消，且取消后已完成状态保留。
- [x] 重复执行是幂等的：已发布资源再发布、已下线资源再下线应标记 skipped，不产生误导性失败。
- [x] 操作完成后，列表、筛选计数、状态计数和展示健康计数统一刷新。
- [x] 操作日志记录动作、筛选快照、影响数量、成功/跳过/失败数量、管理员会话和资源 ID；不得记录完整直链、Token 或 Cookie。
- [x] 用户权限不足、会话过期、接口超时或部分失败时，前端显示明确下一步动作，不得误报全部成功。

### 验收

- [x] 集成测试覆盖“全部、仅照片、仅视频、关键词筛选、展示健康筛选”五种作用域的发布与下线。
- [x] 测试确认只改变指定范围内记录的状态，范围外记录不变。
- [x] 测试确认重复调用返回幂等结果，且操作日志不重复记录误导性变更。
- [x] 测试确认 `excluded` 记录可以被发布或下线，但公开接口仍按展示健康规则剔除。
- [x] 真实容器验证确认进度、结果摘要、刷新后的计数和公开列表行为一致。

## 4. 筛选显示正确性

### 需求

- [x] 类型筛选、状态筛选、展示健康筛选和关键词筛选使用同一套后端谓词生成列表与计数。
- [x] `全部` 计数等于照片数加视频数；`照片` 与 `视频` 计数互斥且不重叠。
- [x] 当前选中筛选必须有清晰的视觉状态和可访问名称；键盘与读屏可识别。
- [x] 筛选条、结果总数、分页总页数和列表行数相互一致。
- [x] 筛选后没有结果时显示空态和清除筛选入口，而不是空白页面或旧数据。
- [x] 加载、刷新和批量操作过程中防止重复提交；过期响应不得覆盖较新的结果。
- [x] 批量发布/下线后仍保留当前筛选，不允许用户被重置回无筛选列表。

### 验收

- [x] 后端测试用固定种子数据验证所有筛选组合的行集与计数。
- [x] 前端测试验证筛选按钮数量、当前选中态、结果数量、分页和空态。
- [x] 真实容器测试对比当前基线：后台索引 65 / 48 / 17，展示健康 64 / 1，公开列表 64 / 47 / 17。
- [x] 批量操作后重新读取计数，确认页面没有显示旧缓存数据。

## 5. 视频浏览器兼容自动筛选

### 需求

- [x] 为视频建立浏览器兼容展示状态，至少区分 `supported`、`unsupported` 和 `unknown`；状态必须持久化，避免每次请求重复探测。
- [x] 公开列表、推荐和详情对视频统一叠加浏览器兼容谓词：只有 `supported` 视频可展示；`unsupported` 与 `unknown` 默认从公开画布剔除。
- [x] 照片不受视频兼容筛选影响；后台索引中的视频总数和远程文件仍保持完整。
- [x] 兼容判定使用保守格式矩阵，优先依据已持久化的容器、编码、profile 和音频编码；没有充分证据时不把视频标记为 `supported`。
- [x] 需要补充探测时使用已有元数据或受控短程探测，不得下载完整源媒体、不得完整转码、不得引入全量扫描。
- [x] 探测结果保存状态、格式摘要、失败原因、探测时间和版本；不保存源媒体副本、完整直链、Token 或 Cookie。
- [x] 管理页支持按 `supported / unsupported / unknown` 筛选，并展示视频总数、浏览器可播放数、不可播放数和未知数。
- [x] 管理员仍能看到不可播放视频及其原因；公开详情接口对不可播放视频返回明确的不可展示语义，不暴露远程地址。
- [x] 展示健康、发布状态和浏览器兼容是三个独立口径；公开视频必须同时满足已发布、展示健康和浏览器可播放。
- [x] 兼容状态变化后同步刷新计数、列表、详情和 Nginx 短缓存；不得让已剔除视频继续出现在缓存响应或推荐中。
- [x] 浏览器兼容矩阵需要登记假设和来源；后续如果要支持 Safari HEVC 等浏览器特有能力，必须按客户端能力细化，而不是默认放宽全站谓词。

### 验收

- [x] 固定种子数据覆盖：可播放 MP4、不可播放编码、未知编码、照片、未发布、展示健康排除和发布状态组合。
- [x] 后端测试确认公开列表、推荐和详情使用同一兼容谓词；后台列表可按兼容状态筛选且计数正确。
- [x] 测试确认兼容状态只影响公开可见性，不删除索引、不修改远程资源、不改变照片展示。
- [x] 测试确认 `unknown` 默认不进入公开展示，且不会因刷新反复触发探测。
- [x] 真实容器验证公开列表与推荐只包含浏览器可播放视频；照片数量和可播放视频数量与 API 计数一致。
- [x] 真实浏览器验证可播放视频能进入查看器；不可播放视频不出现在画布、推荐和旧详情地址的公开展示中。
- [x] 快照记录探测数量、状态分布、资源峰值、临时文件和 Source Zero 审计；无源媒体落盘、无完整转码、无 OOM。

## 6. 首页资源加载显示增强

### 需求

- [x] 卡片在请求前、加载中、加载完成和加载失败四个阶段使用连续状态，不出现整块空白或布局跳动。
- [x] 探索“卡片掠光 / shimmer”作为加载中反馈：掠光只出现在等待派生图的卡片上，加载完成后立即移除。
- [x] 占位必须使用媒体真实宽高或已知比例驱动，保证媒体返回后卡片尺寸、列位和相邻卡片不跳动。
- [x] 掠光不得替代图片：媒体可见后不保留遮罩、模糊层或动画残留；失败态切换为轻量重试卡。
- [x] 掠光运动优先使用 `transform` / `opacity` 合成层动画，不逐帧改布局属性，不引入新的轮询或全屏监听。
- [x] 掠光幅度、频率和对比度保持克制，不遮挡媒体轮廓，不形成大面积重色或霓虹光效。
- [x] 用户开启 `prefers-reduced-motion` 时显示静态柔和占位，不播放掠光动画。
- [x] 缓存命中或响应很快时不得出现闪烁；需要定义最小显示阈值，避免短暂加载被误造成动画抖动。
- [x] 新卡片进入视口、筛选切换、滚动追加和查看器关闭返回时都使用同一加载语义；不会重复请求、清空画布或丢失滚动位置。
- [x] 失败只影响单张卡片；429 / 5xx / timeout 显示可重试状态，不触发整页“回忆库暂不可用”。
- [x] 首页图像排队策略保持有效：可见卡片优先、查看器高优先级、切换或取消时中止不再需要的请求。

### 验收

- [x] Playwright 覆盖等待态、完成态、失败态、重试态和取消态；断言加载完成后掠光层不存在。
- [x] 用延迟响应验证卡片有稳定占位和加载反馈；用缓存或即时响应验证没有短暂闪烁。
- [x] 375 / 768 / 1280 / 1600 视口无横向溢出、无卡片重叠、无文字遮挡。
- [x] CLS < 0.05；同一卡片加载前后宽高一致；相邻卡片不因媒体返回而移位。
- [x] `prefers-reduced-motion` 下通过样式或运行时开关确认掠光动画不执行。
- [x] 真实容器验证首页可见卡片只请求必要尺寸，刷新、滚动追加和查看器返回后的网络行为符合排队策略。
- [x] 采集首屏可见卡片成功率、加载耗时、重试率和动画期间的主线程阻塞证据；改动前后必须有可比数据。

## 7. 页脚常驻与首页媒体总量

### 需求

- [x] 页脚使用固定或粘性位置常驻在视口内；用户在长画布任意滚动位置都能看到并使用页脚入口。
- [x] 页脚位置在首屏、加载中、滚动追加、筛选切换和错误态保持稳定；不得因媒体加载或页码变化跳动。
- [x] 页脚不得遮挡类型筛选、最后一张卡片、查看器控制按钮或页脚自身链接；必要时画布底部预留安全内边距。
- [x] 页脚在移动端与桌面端均可读、可点击；正文对比度达到 AA，链接可点击目标不小于 44×44px，并尊重安全区。
- [x] 首页显示“已收录媒体”总量，至少展示公开可浏览的照片与视频总数；如同时展示照片/视频细分，标签必须写明口径。
- [x] 总量由后端接口提供；前端不得用当前页 `items.length`、已渲染卡片数或滚动页数推导。
- [x] 全局媒体总量不随分页、滚动追加、卡片加载失败或查看器开关变化；筛选后可另示当前结果数，但必须与全局总量清楚区分。
- [x] 公开总量只统计公开可浏览资源，并且与展示健康、浏览器兼容筛选保持同一谓词；不统计或暗示草稿、下线、排除和不可播放资源数量。
- [x] 接口失败或加载中时总量显示骨架、占位或最近一次成功值，不显示误导性的 0；空库时明确显示 0。

### 验收

- [x] 用真实容器和长画布验证滚动到顶部、中部、底部时页脚始终可见，且视口坐标保持稳定。
- [x] 375 / 768 / 1280 / 1600 视口无横向溢出，页脚不遮挡筛选条、最后一张卡片或查看器控制区。
- [x] Playwright 断言页脚链接可聚焦、可键盘激活，文本对比度与可点击目标满足无障碍要求。
- [x] 后端测试确认公开媒体总量、照片数、视频数与公开列表使用同一筛选谓词。
- [x] 前端测试对比页面总量文本和 API 返回值；滚动加载更多时总量不变。
- [x] 切换类型筛选时全局总量保持稳定；如显示筛选结果数，两个数字的口径标签可被读屏识别且不混淆。
- [x] 排除一条资源或新增一条公开资源后，真实容器总量、列表和计数同步刷新；前端不显示过期数据。

## 8. 工程门禁

- [x] 后端 pytest 全量通过。
- [x] 前端 typecheck 和 production build 通过。
- [x] Playwright 覆盖计数、筛选、视频兼容剔除、确认弹窗、批量进度、幂等结果、失败态、首页加载显示、页脚常驻和公开收录总量。
- [x] 视觉令牌仍只在 `frontend/src/index.css`；品牌与界面文案仍只在 `frontend/src/lib/brand.ts`。
- [x] 正文对比度达到 AA；可点击目标不小于 44×44px。
- [x] `docker compose config`、whitespace / `git diff --check` 和设计来源检查通过。
- [x] 真实 Nginx 链路通过，backend / db / redis / nginx 全部 healthy。
- [x] backend 内存上限保持 256m；批量操作不得引入源媒体下载或完整转码。

## 9. 交付顺序

1. 先锁定筛选谓词和计数契约，补齐接口测试。
2. 再持久化视频浏览器兼容状态，并把 `supported` 纳入公开视频谓词。
3. 再实现后台计数展示与筛选一致性。
4. 最后接入一键发布/下线、确认弹窗、任务进度和操作日志。
5. 首页加载显示可与后台计数并行设计，但必须独立提交、独立验收，不与批量操作混在同一回滚单元。
6. 页脚常驻与首页总量展示可并行设计，但先补齐公开计数契约和布局断言，再接入页面。
7. 每一步都先过自动化门禁，再做真实容器验收。

## 10. Ledger

| ID | 事项 | 状态 | 结论 |
| --- | --- | --- | --- |
| C0 | 清单定版 | 2026-09-07 完成 | 定版后台计数、批量发布/下线、筛选一致性、浏览器可播放视频自动筛选、首页加载显示、页脚常驻和公开收录总量展示；未实施功能。 |
| C1 | 后台媒体总数与分类计数 | 2026-09-07 完成 | 管理页展示全局索引、状态、展示健康和兼容分布；测试与真实容器确认计数不受分页和筛选影响。 |
| C2 | 一键发布与一键下线 | 2026-09-07 完成 | 后台按筛选快照异步分批执行；确认弹窗、进度、取消、幂等结果、失败恢复建议和操作日志落地。 |
| C3 | 筛选显示正确性 | 2026-09-07 完成 | 列表、分页和计数共用后端谓词；过期响应、重复提交和旧缓存已防护。 |
| C4 | 首页卡片掠光与加载状态增强 | 2026-09-07 完成 | 卡片连续加载态、150ms 最小展示阈值、合成层掠光、失败重试和 `prefers-reduced-motion` 降级通过 Playwright。 |
| C5 | 浏览器可播放视频自动筛选 | 2026-09-07 完成 | 持久化保守兼容矩阵、受控短程探测、公开谓词统一剔除 unsupported/unknown，并同步失效 Nginx 缓存。 |
| C6 | 页脚固定位置与长画布可达性 | 2026-09-07 完成 | 页脚 fixed 常驻，375/768/1280/1600 视口滚动坐标稳定且无横向溢出。 |
| C7 | 首页公开收录媒体总量展示 | 2026-09-07 完成 | `/memories/public-counts` 与公开列表/推荐/详情共用谓词；页面显示 47 / 47 / 0 并与 API 一致。 |
