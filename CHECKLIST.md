# Li&Media Single-Gateway Transfer & Experience Checklist

> 更新于 2026-09-06。单网关传输与体验优化已实施，本清单保留为验收基线和后续回归清单。
> 目标口径：编排内只保留一个 Nginx 作为唯一服务入口，由它统一暴露前端、API、健康检查和公开派生图；在此入口上叠加传输节省、缓存策略和体验优化。前端静态资源、API、派生图共用同一个域名和端口，后端、数据库、Redis 不直接暴露。
> 边界约束：继续只索引百度网盘资源，不保存媒体本体；百度直链、凭证和用户数据不入库、不持久化到前端存储；Nginx 不得缓存短时直链或任何带凭证的响应；公开派生图和静态资源才允许长缓存。

## 1. 唯一 Nginx 入口与编排

- [x] 复核 Compose 服务边界：保留唯一对外暴露的 `nginx` 网关服务，前端构建产物、API 反代和健康检查都由它提供。
- [x] 收敛宿主端口：仅 Nginx 暴露 `$HTTP_PORT` / `$HTTPS_PORT`；backend、db、redis 不配置宿主端口映射。
- [x] 明确内部访问路径：Nginx 通过 Compose 网络访问 `backend:8000`，不通过宿主回环地址访问后端。
- [x] 统一路由：`/` 提供 SPA 静态资源，`/api/v1` 反代后端，`/api/docs` 与 `/api/openapi.json` 可按环境关闭或保护。
- [x] 统一健康入口：提供 Nginx 自身健康检查，并保留后端 `/api/v1/healthz` 与 readiness 代理路径。
- [x] 依赖健康：Nginx 启动依赖 backend healthy；backend 依赖 db / redis healthy。
- [x] 持久化边界清晰：数据库、Redis、媒体派生缓存、服务端凭证配置分别挂载，不因重建 Nginx 丢失。
- [x] `docker compose config` 验证只有预期服务暴露宿主端口，且没有服务名冲突、循环依赖或重复挂载。

## 2. Nginx 传输节省与缓存边界

- [x] 为静态资源设置长缓存：带内容哈希的 JS/CSS/图片使用 immutable 缓存；`index.html` 使用短缓存或协商缓存。
- [x] 为 API JSON 开启压缩，覆盖 `application/json`、HTML、CSS、JS、SVG 等文本类型。
- [x] 评估并启用 Brotli 或更高压缩级别；如 Nginx 镜像不支持 Brotli，则在清单中明确保留 Gzip，不引入额外网关。
- [x] 为 480px / 1280px 公开派生图设计 Nginx `proxy_cache`：缓存键包含媒体 ID、派生版本、尺寸和 `Accept` 相关维度。
- [x] 公开派生图保留后端 ETag、`If-None-Match`、`304 Not Modified`，Nginx 不得破坏协商缓存。
- [x] 禁止缓存 `direct-url`、登录、登出、管理接口、带 Authorization / Cookie 的私有响应。
- [x] 短时直链响应必须保持 `Cache-Control: no-store`，Nginx 不能把百度直链写入磁盘缓存或内存缓存。
- [x] 为服务端媒体流回退路径保留 `Range` 支持；必要时关闭缓冲，避免大文件回退占用 Nginx 内存或磁盘。
- [x] 定义缓存空间上限、清理策略和命中日志，避免派生图缓存无限增长。
- [x] 定义接口限流和并发保护：公开列表、派生图、播放回退路径分别设置合理限速，避免快速点击打满带宽。

## 3. 图片传输预算与自适应派生

- [x] 重新定义首屏媒体传输预算：按 360 / 768 / 1280 / 1600 视口分别设定首页 18 张卡片的图片传输上限。
- [x] 设计 240 / 480 / 768 / 1280px 派生尺寸矩阵，首页按列宽和 DPR 选择最小可用尺寸，而不是一律请求 480px。
- [x] 查看器按视口选择大图尺寸：移动端请求更小的大图，桌面端继续使用 1280px；避免移动端下载桌面级大图。
- [x] 图片 `srcSet`、`sizes`、DPR 和布局宽度必须在 Playwright 中可断言，避免浏览器继续隐式命中高尺寸缓存。
- [x] 首图可优先加载；其余卡片保持懒加载，且懒加载不得造成布局跳动。
- [x] 派生图 URL 继续包含派生版本；压缩参数、尺寸矩阵或裁剪规则变化时必须更新版本。
- [x] 记录单张派生图平均体积和最大体积，为后续选择 WebP / AVIF / 质量等级提供数据。
- [x] 若引入 AVIF 或更高质量档位，必须保持浏览器回退到 WebP，且不允许为生成新格式而把原始媒体持久化到本地。

## 4. API 与列表负载优化

- [x] 审计公开列表响应字段：首页卡片不需要的字段不应重复传输，例如不参与 UI 的详细文件元数据。
- [x] 设计列表瘦身 DTO：首页列表只保留渲染、筛选、打开查看器和分页所需字段。
- [x] 查看器优先复用列表项；只有刷新、旧链接或列表中不存在时才按 ID 请求单条数据。
- [x] 列表页与查看器共享同一内存数据源，避免同一媒体在一次会话中重复拉取详情。
- [x] 设计分页传输优化：保持当前分页体验的前提下减少重复 JSON 字段；不引入需要预下载全部索引的模式。
- [x] API 响应开启压缩后，重新评估 `page_size=18` 的传输体积和后端序列化开销。
- [x] 公开列表、类型计数和推荐接口设计稳定缓存边界；公开响应可被 Nginx 短缓存或即时绕过，管理接口必须绕过缓存。

## 5. 视频播放与直链体验

- [x] 视频首页只加载 480px 海报，继续禁止请求视频本体。
- [x] 查看器打开时先展示 1280px 海报；用户触发播放后才请求百度短时直链。
- [x] 直链仅保存在组件内存，临期、403、404 或播放失败时重新请求 `direct-url`。
- [x] 服务端媒体流回退只作为直链失败兜底；Nginx 对该路径保持流式转发，不缓存响应体。
- [x] 同一时间只允许一个视频播放任务；切换媒体或关闭查看器时停止播放、清空 `src` 并释放解码资源。
- [x] 播放失败状态提供明确的重试和回退动作，不出现静默黑屏。
- [x] 记录真实视频的首帧时间、直链刷新次数和回退次数，作为后续优化基线。

## 6. 查看器与交互体验

- [x] 查看器继续使用 URL 驱动；刷新、前进、后退、分享链接后能直接打开对应媒体。
- [x] 照片大图保持等比缩放，不被页眉、页脚、状态条、导航按钮或下载按钮遮挡。
- [x] 视频大图按媒体宽高比适配屏幕，播放器、海报和控制组件不被页眉/页脚遮挡。
- [x] 左右切换保持当前画布顺序、类型筛选和滚动状态；切换后 URL、状态和媒体数据保持一致。
- [x] 关闭查看器后恢复首页滚动位置和筛选状态，浏览器历史行为符合用户预期。
- [x] 加载占位、错误态、重试按钮、关闭按钮、导航按钮和下载按钮均满足对比度与 44×44px 热区要求。
- [x] 快速连续打开、切换或关闭查看器时不得产生并发请求风暴。
- [x] 明暗主题下检查查看器、背景模糊、状态条、错误态和媒体容器对比度。

## 7. 后台与运维体验

- [x] 后台列表、扫描状态、派生缓存和清理入口继续只通过唯一 Nginx 入口访问。
- [x] 管理接口禁用公开缓存；后台静态资源仍可长缓存。
- [x] 保留确认式一键清理能力，并在清理后同时失效应用缓存和 Nginx 派生图缓存。
- [x] 设计缓存指标或管理视图：派生图数量、Nginx 缓存大小、命中率、回源次数和清理后的空态。
- [x] 操作日志继续不得包含完整直链、Token、Cookie 或敏感请求参数。
- [x] 部署重建后验证本地索引、派生缓存、数据库和 Redis 持久化不受影响。

## 8. 安全与发布边界

- [x] Nginx 设置安全响应头：`X-Content-Type-Options`、`Referrer-Policy`、基础 `Content-Security-Policy`、必要的 HSTS。
- [x] 生产环境默认关闭或保护 API 文档；如需保留，必须使用环境开关控制。
- [x] Nginx 不得记录完整百度直链、Authorization、Token、Cookie 或敏感查询参数。
- [x] `client_max_body_size`、超时时间、连接数限制按当前无本地上传、只有 API 和派生图代理的场景设置。
- [x] TLS / HTTPS 设计为可配置项：开发环境可用 HTTP，生产环境由同一 Nginx 终止 TLS。
- [x] 更新 `.env.example` 与部署说明，明确唯一入口端口、健康检查地址、缓存目录和持久化卷。

## 9. 自动化测试与验收

- [x] Playwright 覆盖唯一入口：首页、静态资源、API、健康检查都通过同一 Nginx 域名访问。
- [x] Compose 验收确认 backend、db、redis 没有宿主端口，只有 Nginx 暴露服务。
- [x] Playwright 断言首页只请求所选自适应尺寸，不请求 1280px；查看器打开后才请求所选大图。
- [x] Playwright 断言静态资源命中长缓存，重复访问命中 304 或磁盘缓存；派生图响应保留稳定 ETag。
- [x] Playwright 断言 `direct-url` 响应为 `no-store`，Nginx 不缓存短时直链。
- [x] Playwright 覆盖照片放大、视频播放失败恢复、左右切换、Esc 关闭、滚动和筛选恢复。
- [x] Playwright 覆盖后台清理确认、执行反馈、缓存空态和网盘资源不变。
- [x] 收集 360 / 768 / 1280 / 1600 视口截图，检查无横向溢出、无文本溢出、无布局位移。
- [x] 明暗主题检查公开页、后台、查看器、空态、错误态和按钮对比度达到 AA。
- [x] 记录优化前后指标：首屏传输体积、图片总传输体积、API JSON 体积、LCP、CLS、缓存命中率和 304 数量。

## 10. 质量门禁与合并

- [x] 后端 pytest 全量通过。
- [x] 前端 typecheck 与 production build 通过。
- [x] 设计令牌、品牌文案来源检查通过。
- [x] `docker compose config` 通过，并完成单入口端口审计。
- [x] Playwright 冒烟、传输链路和体验用例全部通过。
- [x] `git diff --check` 通过；本地任务分支保留合并提交合回 `main`。
