# Li&Media 设计验收与测试通过标准

> 版本：V1.0 ｜ 更新：2026-09-08 ｜ 状态：长期验收基线
>
> 本文档不是已完成事项清单。每次交付前逐项判定“通过 / 不通过”，所有必测门禁都必须有证据；任何一项不通过，功能不能合入 `main`。
> 视觉与品牌依据 [BRAND.md](design-system/limedia/BRAND.md) 和 [MASTER.md](design-system/limedia/MASTER.md)；实现与文档冲突时以代码为准，并回写设计方案。

## 1. 验收等级

| 等级 | 定义 | 处理 |
| --- | --- | --- |
| P0 | 崩溃、数据丢失、越权访问、内容或状态错误、无法完成核心路径 | 必须修复后重跑全部门禁 |
| P1 | 明显布局错位、媒体闪烁或跳动、错误状态误导、无键盘/读屏路径、性能或安全门禁超限 | 必须修复；不得以“可跟踪”合入 |
| P2 | 文案不够自然、局部间距偏移、非核心状态提示可优化 | 可记入 backlog；不阻塞当前交付 |
| Blocker evidence | 缺少截图、指标、日志或测试证据 | 视同对应项不通过 |

核心路径固定为：公开首页加载、类型筛选、无限滚动到底提示、打开照片/视频查看器、播放/下载、关闭后滚动恢复、后台发布/下架、状态与资源观测。

## 2. 产品与内容验收

### 通过标准

- 只展示 `photo` 和 `video`；不出现电影、剧集、动漫、纪录片、音乐 MV 或评分元数据。
- 公开列表、推荐、详情、派生图、播放和下载只处理 `published` 且 `public_display_state=displayable` 的资源。
- 新增公开内容必须同时提供稳定标题/替代文本、可理解状态；不允许用技术路径、UUID 或哈希作为主要展示信息。
- 空库、筛选空、加载中、加载失败、分页失败、到底、查看器缺失、视频编码不兼容都有独立且不误导的文案。
- 失败提示可操作：受影响卡片或分段提供重试；不把局部失败显示成整库不可用。
- 底部到达最后一页时明确显示“已到底”，并给出当前筛选总数；成功加载更多时提示自然消失。
- 状态文案只表达用户视角：排队中、加载中、已就绪、缓冲中、失败可重试、编码不兼容；不暴露任务 ID、远端路径、失败堆栈、凭证或内部队列明细。

### 证据

- Playwright 断言空库、错误、重试、到底和查看器恢复状态。
- 手工或 API 测试确认草稿、下架、排除、不支持视频不在公开响应。
- 公开响应扫描无凭证、主机路径、远端目录、内部任务 ID 和堆栈。

## 3. 视觉与品牌验收

### 通过标准

- 颜色、阴影、圆角、缓动、动效时长只来自 `frontend/src/index.css` 的 `--limedia-*` 令牌。
- 品牌名、导航、界面文案、空态和错误文案只来自 `frontend/src/lib/brand.ts`；组件不硬编码品牌短语。
- 主色只做小面积强调；无霓虹渐变、AI 紫粉、粉色系、大面积重色或夸张玻璃拟态。
- 卡片、空态、查看器、状态徽标、筛选条和页脚在浅色与深色主题下边界清晰、无重叠、无文字截断。
- 媒体占位使用真实宽高或已知比例；加载完成后同一卡片宽高、列位和相邻卡片不移动。
- 入场动效只发生一次；环境动效极慢、极淡；加载/状态动效只使用 `opacity`、`transform` 或 `background-position` 等可合成属性。
- `prefers-reduced-motion: reduce` 下不播放掠光、持续环境动画和非必要位移。
- 状态色使用语义令牌，不在组件里猜测映射。

### 证据

- `scripts/check.py` 的设计令牌与品牌来源检查通过。
- 浅色/深色主题截图覆盖 375、768、1280、1600px；无横向溢出、遮挡、截断或失控对比。
- Playwright 断言减动效后动画名为 `none`，动画期间无布局属性变化。

## 4. 响应式与无障碍验收

### 通过标准

- 375 / 768 / 1280 / 1600px 必测；320、390、769、1024px 作为发布抽查。
- 瀑布流列数按设计断点变化：375 两列、768 三列、1024 四列、1280 五列、1600 六列。
- 页眉、筛选、画布、底部提示、页眉页脚和查看器控件互不遮挡；窄屏文本优先换行而不是截断关键内容。
- 所有可交互控件有可见焦点、语义名称和明确操作；触控目标至少 44×44px。
- 底部加载/失败/到底提示、预热状态和查看器状态可被读屏感知；非紧急更新使用 `aria-live="polite"`，用户主动触发或错误恢复可用 `assertive`。
- 弹层和查看器有焦点管理、Escape 关闭和关闭后焦点/滚动恢复。
- 正文和状态文本对比度达到 WCAG AA；图标必须有可见文本或 `aria-label`。

### 证据

- Playwright 覆盖四视口无横向溢出和关键元素不重叠。
- 浏览器无障碍树检查通过：控件名称、`aria-live`、`aria-pressed`、`aria-modal` 和焦点顺序正确。
- 对比度检查或样式计算结果确认明暗主题正文/状态文本均 ≥4.5:1。

## 5. 加载与媒体体验验收

### 通过标准

- 首屏关键图优先加载，非视口图懒加载；全局图像队列有界，切换、筛选或关闭时取消无效请求。
- 卡片在排队、占位、加载、完成、失败之间连续变化；快响应不闪烁，慢响应才显示掠光。
- 加载失败不清空画布；已有卡片、筛选状态和滚动位置保持不变。
- 无限滚动在接近底部时追加，重复项去重；失败时保留“重试加载”，不重复并发请求。
- 到最后一页时进入“到底”提示；筛选或刷新后按新结果重新判定。
- 照片查看器支持小图先行、大图替换、键盘、手势、缩放、下载和重试；视频在用户触发播放后才进入播放链路。
- 播放准备、缓冲、可播放、失败和编码不兼容状态不重叠；同一时间只显示最高优先级状态。
- 缩略图矩阵至少覆盖 240、480、768、1280px；首页选择最小可用尺寸，移动端查看器用 480px，桌面查看器用 1280px。
- 公开列表响应可短缓存，直链和流不缓存；派生图可长缓存并带稳定版本。

### 证据

- Playwright 覆盖骨架列、首图优先、懒加载、队列并发上限、滚动追加、到底、失败恢复、查看器和明暗视口。
- CLS < 0.05；LCP 目标：本机/局域网 < 2.5s，真实网络按部署环境记录趋势。
- 24 媒体页传输和请求预算通过；异常网络下核心内容仍可回退展示。
- 记录改动前后首图耗时、首屏图片成功率、重试率、长任务或主线程阻塞证据。

## 6. API、安全与状态一致性

### 通过标准

- 所有新接口落在 `/api/v1`；公开接口不返回内部管理数据、远端目录、任务 ID、失败原因或凭证。
- 公开数据谓词必须一致：列表、推荐、详情、计数、派生图、播放和预热统计不得出现不同可见范围。
- 百度网盘直链只由服务端短期签发；前端不持久化直链，响应与浏览器存储不得保留可复用凭证。
- 带凭证或管理能力的接口必须鉴权；未登录访问返回安全错误，不泄露资源存在性。
- 后台状态只对管理员开放；公开预热摘要只包含安全状态和进度。
- 429 返回 `Retry-After`；5xx、远端失败和不可读路径有明确状态，不伪装为成功或零值。
- 资源限制配置保留默认值、上下限和显式 `0` 语义；非法值失败，不静默绕过容器硬限制。

### 证据

- 后端 pytest 覆盖公开可见性、404/502/429、鉴权、配置边界、缓存口径和响应字段。
- 集成测试确认管理员鉴权后的接口与公开接口数据边界分离。
- `docker compose config` 通过；Nginx 契约测试确认缓存、限流、健康检查和安全头。

## 7. 后台与资源验收

### 通过标准

- 后台登录、退出、会话失败、批量发布/下架、扫描、预热、状态刷新、错误重试和删除确认可用。
- 批量状态操作可预览、确认、取消、幂等重复执行，并在操作日志中记录变更与跳过数量。
- 状态面板显示后端栈、远端索引、任务队列、文件系统、派生缓存、临时文件和生效资源限制；不可用项显示不可用而不是零。
- 磁盘目录扫描有文件数和超时边界；缓存、临时文件和 Nginx 缓存可分项解释。
- backend 内存上限保持 256m；提升并发后必须重新记录 cgroup、子进程、OOM、PID 和临时文件基线。
- 清理流程有显式确认、事务回滚、文件清理和直链缓存失效；百度网盘资源不被修改。

### 证据

- 真实容器四服务 healthy；Nginx 唯一入口健康接口返回 200。
- 资源基线显示 OOM=0、临时文件残留=0、cgroup 峰值低于 256m、PID 上限内稳定。
- 配置重启后后台显示生效值；公开预热状态基于持久化缓存命中收敛，不因内存任务历史误报。

## 8. 自动化测试通过标准

| 命令 | 通过标准 |
| --- | --- |
| `cd backend && pytest -q` | 0 failed，0 error；新增后端行为有正例、边界、失败和越权用例 |
| `cd frontend && npm run typecheck` | TypeScript 0 error |
| `cd frontend && npm run build` | Vite production build 成功；无新增 blocking warning/error |
| `python3 scripts/check.py` | 后端、前端、Compose、whitespace、设计来源和 Playwright 全部通过 |
| `python3 scripts/check.py --skip-e2e` | 仅允许在暂无浏览器环境时作为中间检查；合入/交付前仍必须跑完整版 |
| `docker compose config --quiet` | 配置可解析；环境变量、服务边界和健康检查有效 |
| `git diff --check` | 无 whitespace error |
| `cd frontend && npx playwright test` | 全部通过；失败用例必须先修复或说明为无效需求后删除，不得长期跳过 |

最低自动化覆盖：

- 后端：公开可见性、接口瘦身、安全头、鉴权、状态一致性、派生失败、预热状态、资源边界、配置校验和 Compose 契约。
- 前端：骨架、懒加载、队列、滚动追加、到底、筛选、查看器、视频状态、减动效、四视口、无横向溢出、后台关键工作流。
- 新增 UI 状态必须有自动化断言；新增接口必须有后端契约测试；新增缓存或限流必须有命中/绕过/过期测试。

## 9. 真实链路与发布通过标准

### 必过项

1. 本地全量门禁通过。
2. Compose 构建或重建后 `backend`、`db`、`redis`、`nginx` 全部 healthy。
3. Nginx 唯一入口返回首页 200，`/healthz` 正常，静态资源缓存头正确。
4. 公开首页冷缓存与热缓存都完成验收：首屏无空洞，失败可重试，预热状态收敛。
5. 照片和视频样本在真实 Nginx 链路可打开；视频播放、Range、失败回退和重试通过。
6. 后台登录、扫描/预热入口、状态刷新、批量操作和清理确认通过。
7. 明暗主题、375 / 768 / 1280 / 1600 视口、减动效、键盘路径和读屏语义通过。
8. 公开响应无内部路径、凭证、任务 ID、堆栈或非公开资源信息。
9. 资源基线无 OOM、无临时文件残留，队列归零，磁盘指标可解释。
10. README、`design-system/limedia`、`.env.example` 和运行说明与实现一致。

### 证据包

交付说明至少记录：

- 执行命令、日期、分支、提交或运行镜像。
- 后端 pytest、前端 typecheck/build、Playwright 的最终计数。
- 四视口截图或可复核链接。
- 真实容器健康、公开预热状态、资源峰值、OOM 和临时文件结果。
- 已知 P2 项、不解决的问题及后续跟踪入口；P0/P1 不允许遗留。

## 10. 缺陷与变更规则

- 只修复当前验收范围内的缺陷；无关格式化、重构和依赖升级不得混入。
- 行为变更先更新接口契约、品牌文案、设计文档和本标准的对应验收项，再实现。
- 新增资源消耗、公开状态、页面状态或交互状态前，先补测试与验收口径。
- 视觉、状态机或加载策略变更必须保留旧状态的可回滚路径，并在同一验收轮补齐新旧对比。
- 测试不稳定时先隔离原因；不得通过提高 timeout、跳过用例或放宽预算让门禁变绿。

## 11. 公开站体验优化方案

> 状态：待实施。以下方案不降低第 1–10 节标准；每项进入实施时先补契约/自动化用例，再改动 UI 或 API。一次任务只做一个优化项，避免回滚范围扩大。

### O1：列表响应与访问链路提速

**目标**：让回访和缓存命中时的首页更快显示，同时不缓存管理会话或过期公开结果。

**方案**

- `GET /api/v1/memories` 返回明确的短时缓存语义，并与 Nginx 15 秒网关缓存对齐；候选语义为 `public, max-age=15, stale-while-revalidate=30`。
- 带管理会话或凭证的请求不得共享缓存；服务端或网关必须回退为 `no-store` / `private`，Nginx 继续按现有 cookie/authorization 判断绕过。
- 推荐、预热状态、直链、播放和流式回退保持 `no-store`，不参与短时公开缓存。
- 缓存键保留 `kind`、`page`、`page_size` 等查询参数；发布、下架或清理后不需要立即失效，但必须在 15 秒内自然收敛。

**通过标准**

- 后端契约测试断言无会话请求的缓存头、带会话请求的不缓存行为。
- Nginx 契约测试确认网关缓存键、`proxy_cache_bypass` / `proxy_no_cache` 与后端语义一致。
- Playwright 验证首页首次加载、筛选变更和二次加载正常；不出现跨筛选错误复用。

### O2：查看器 chunk 预取

**目标**：减少首次点击媒体后等待 `MediaViewer` 动态 chunk 下载的时间。

**方案**

- 首页列表完成渲染且浏览器空闲时预取 `MediaViewer` chunk；卡片 pointerenter 或 focus 时可再次触发，但不阻塞当前任务。
- 不预取管理后台 chunk，不在首屏关键路径同步加载查看器，不为预取引入第三方脚本或全局轮询。
- 预取失败静默降级；用户点击时仍按现有 lazy + Suspense 流程加载。

**通过标准**

- Playwright 断言点击前查看器 chunk 已请求，或使用受控网络验证 pointerenter/focus 预取。
- 首屏关键请求数量和传输量不因预取上升；`AdminPage` chunk 不被公开首页预取。
- 键盘 focus 到卡片后点击 Enter/Space 能正常打开查看器。

### O3：预热状态轮询收敛

**目标**：保留必要的状态反馈，同时降低 `ready` 后的无效请求和“预热”这类内部运维词汇对访客的干扰。

**方案**

- `running`、`not_preheated`、`degraded` 期间继续轮询；进入 `ready` 后停止固定轮询。
- 页面从隐藏回到可见时刷新一次；接口失败使用有限退避，不在后台无限高频重试。
- 访客文案改为用户视角：“正在优化预览”“首次加载可能稍慢”“预览已就绪”；`ready` 和接口不可用可弱化或隐藏。
- 继续只展示安全进度，不展示任务 ID、队列明细、失败原因或远端资源信息。

**通过标准**

- Playwright 覆盖轮询停止、页面重新可见刷新、失败退避和状态收敛。
- 公开接口失败不误报成功；状态更新可被 `aria-live="polite"` 感知。
- 网络面板确认 `ready` 后无固定 20 秒轮询。

### O4：回忆库底部三态提示

**目标**：让访客明确知道当前处于加载中、失败可重试，还是已经看完。

**方案**

- 新增 `CanvasFooterState = hidden | loading | retry | end`，与首屏错误、空库状态分离。
- 追加加载显示“正在加载更多回忆”；分段失败保留“更多回忆加载失败”和“重试加载”。
- 当 `!hasNextPage`、无加载错误且 `memories.length > 0` 时显示“已经到底了”；总数使用当前筛选的 `total`，不把随机推荐数量计入。
- 成功到底后提示保持安静、可读；可提供“回到顶部”入口，但不使用打断式告警。

**通过标准**

- Playwright 覆盖加载中、追加失败、重试成功、重复分页去重和最后一页到底。
- 底部提示使用 `role="status"` 与 `aria-live="polite"`；375 / 768 / 1280 / 1600 视口不遮挡页脚和卡片。
- 筛选切换后底部状态和总数按新筛选重新计算。

### O5：公开媒体统一状态机

**目标**：把卡片预览、照片查看、视频播放和下载从分散布尔值收敛为用户可理解的连续状态。

**方案**

- 建立共享 `MediaLifecycleState`：`idle -> queued -> loading -> ready -> buffering -> ready`；失败进入 `retrying / failed`，视频浏览器不兼容进入 `unsupported`。
- 状态按作用域区分 `preview`、`playback`、`download`；同一时间只显示最高优先级状态。
- 卡片成功后保持安静，只显示排队中、加载中或失败可重试；查看器可显示更完整的当前阶段。
- `VideoPlayer` 将缓冲事件上抛给查看器状态机；播放准备、缓冲、可播放、失败和编码不兼容不得同时出现多个冲突提示。
- 公开端只展示安全文案：排队中、加载中、已就绪、缓冲中、失败可重试、编码不兼容；网盘错误原因保留在管理端。
- 所有文案进入 `brand.ts`，状态色和图标使用既有语义令牌。

**通过标准**

- Playwright 覆盖照片加载/失败/重试、视频准备/缓冲/失败/编码不兼容、下载和状态切换。
- 每个状态有稳定测试 ID 或语义标签；同一时间最多一个高优先级提示。
- 明暗主题对比度、焦点环、触控目标和 `prefers-reduced-motion` 验收通过。

### O6：公开 DTO 再瘦身

**目标**：进一步减少公开接口暴露的内部索引字段，让公开状态机只依赖访客需要的数据。

**方案**

- 公开 `MemorySummaryRead` / `MemoryRead` 不再返回 `remote_id`、`remote_path`、`parent_path`、失败原因和内部同步状态。
- 保留查看器必需字段：媒体类型、尺寸、时长、标题、替代文本所需信息、`file_url`、`thumbnail_url` 和下载文件名。
- 管理端 DTO 继续保留完整远程状态，用于诊断和重试；不因公开瘦身降低后台可观测性。
- 先更新公开响应契约与前端类型，再删除字段；不做静默字段迁移。

**通过标准**

- 后端测试扫描公开列表、详情、推荐和计数响应，确认没有远端路径、远端 ID、失败原因或内部任务字段。
- 管理端测试确认诊断字段仍然完整。
- 前端 typecheck、build 和查看器/下载/状态机用例通过。

### 交付顺序与总门禁

1. O4 底部三态提示：纯前端状态与文案，风险最低，先建立用户可感知的完整浏览闭环。
2. O5 统一媒体状态机：先抽公共状态模型，再迁移卡片和查看器；不和视觉大改混在一个提交。
3. O3 预热状态轮询收敛：补前端网络断言和公开文案验收。
4. O1 列表短缓存：先契约测试，再改服务端/Nginx；交付前必须验证带会话请求不共享缓存。
5. O2 查看器预取：在功能稳定后做传输优化，避免掩盖状态机问题。
6. O6 公开 DTO 瘦身：最后做契约裁剪，确保前端已不依赖内部字段。

每项完成定义：

- 第 1–10 节相关验收全部通过。
- 新增状态、缓存、DTO 或交互有自动化用例。
- 四视口、明暗主题、键盘路径和减动效验收通过。
- `scripts/check.py` 完整通过，Playwright 0 failed。
- 真实容器冷/热缓存链路、媒体播放、状态提示和资源基线复核通过。
