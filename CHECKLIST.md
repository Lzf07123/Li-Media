# Li&Media 自适应媒体体验 Checklist

> 更新于 2026-09-07。本清单取代“并发限制与视频解码策略 Checklist”。
> 当前阶段的目标不是把固定条目勾完，而是建立一套可重复的迭代流程：先证明问题，再做最小改动，用数据验收，最后回写结论。
> 媒体资源数量假设为未知值 `N`；所有方案必须说明在 `N` 增长到 100、1k、10k、100k 时如何退化，而不是依赖当前 65 条资源的体感。
> “完美版本”不是一个模糊目标：它表示所有 P0 不变量在真实容器、真实请求和真实存储审计下连续两轮全部通过，且没有未解释的 P0/P1 缺陷。

## 0. 迭代规则

- [x] 每项工作必须有稳定编号、假设、验收口径、证据和决策，不允许“已优化”这种不可验收结论。
- [x] 单轮最多保留 3 个进行中实验；超出范围必须进入 Backlog，避免同时改并发、缓存、UI 和扫描导致无法归因。
- [x] 每轮开始先刷新基线快照：资源数量、失败数量、请求错误率、内存峰值、队列指标。
- [x] 每轮只选一个主变量；若必须多变量，先拆成可独立回滚的提交。
- [x] 每轮结束必须得出四种结论之一：采纳、继续观察、调整参数、回滚。
- [x] 结论未写入本清单的 Ledger 前，任务不得标记完成。

## 0A. 自主收敛到完美版

每一轮都必须走同一个闭环；禁止只改代码不做真实容器验证，也禁止把静态 checkbox 当成完成。

```text
Snapshot -> Verify invariants -> Pick highest P0/P1 gap -> One reversible change
       -> Real container + real access test -> Score gates -> Adopt / adjust / rollback
       -> Append Ledger -> Recalculate perfect score -> Repeat
```

### Perfect Score

| Gate | 判定 | 0 分 | 1 分 | 2 分 |
| --- | --- | --- | --- | --- |
| G0 Source Zero | 服务端是否只有索引与已批准派生图 | 存在源媒体/数据库 BLOB/本地 `source_path` | 有遗留文件但已被隔离且无 DB 引用 | 磁盘、DB 和 API 审计全部为零 |
| G1 Real Chain | 真实容器链路是否可用 | 首页或详情不可用 | API 可用但浏览器链路失败 | 4 容器 healthy + API + 浏览器 + 派生图 + 播放边界全通过 |
| G2 Resource Safety | 是否稳定不 OOM | 近期 backend Killed/OOM | 有降级但资源峰值超标 | cgroup peak < 200m，OOM = 0，临时文件为空 |
| G3 Experience | 用户是否能继续浏览 | 整页不可用 | 部分卡失败但可重试 | 失败单卡化、退避重试、CLS < 0.05 |
| G4 Observability | 失败是否可解释 | 只有 4xx/5xx 状态码 | 有部分指标 | 可区分 429/502/MOV/编码/磁盘/百度限流 |
| G5 Scale Readiness | 是否支持未知 `N` | 存在无界接口或全量加载 | 当前规模可用，P1 方案未验 | P0/P1/P2 包线测试通过 |

**Perfect = 12/12，且 G0、G1、G2 连续两轮真实容器运行均为 2 分。**

### 每轮强制输出

- [ ] Snapshot JSON 或表格：容器状态、DB 数量、派生状态、错误率、资源峰值、存储文件分类。
- [ ] 本次改动唯一主变量与回滚命令。
- [ ] 真实访问证据：HTTP 状态、响应头、浏览器请求数、派生图 Content-Type。
- [ ] Source Zero 证据：`source_path` 数、源媒体扩展名计数、`media/photos` 文件数、BLOB 数。
- [ ] Ledger 行：假设、实验、结果、决策。

### 停止迭代条件

连续两轮满足以下全部条件后，进入维护模式；之后只在触发回归时重新开循环：

- [ ] Perfect Score = 12/12。
- [ ] G0/G1/G2 连续两轮通过。
- [ ] 没有未分类的 429/502/派生失败。
- [ ] 没有源媒体文件、本地 source path、DB BLOB 或残留直链。
- [ ] 最新真实浏览器模拟中首页、查看器、缩略图、播放边界全部通过。

## 1. 不可妥协边界

这些是“范围不变量”。任何实验都不能为了性能放松；如果需要修改，必须先开新的风险评审。

- [x] 内容类型只有照片和视频，不引入电影、剧集、动漫、纪录片或音乐 MV。
- [x] 百度网盘只作为远程存储源；服务端不持久化媒体本体，前端不长期保存直链。
- [x] 服务端磁盘只允许保存：数据库索引、公开派生 WebP、必要临时文件和安全配置；不允许保存照片、视频、音频或其他源媒体副本。
- [x] 凭证、Token、Cookie、完整直链和用户敏感数据不入库、不入日志、不进前端存储。
- [x] 服务端视频只提取单帧等比 WebP；不做完整转码，不逐帧扫描。
- [x] backend 的 256m 内存上限不因短期 OOM 放宽；优先减少并发、缩小派生尺寸或迁移到独立 worker。
- [x] Nginx 不缓存短时直链、带凭证响应或私有列表；只有公开且 immutable 的派生图可长缓存。
- [x] 操作日志可以记录数量、状态和资源 ID，但不得包含完整直链、Token、Cookie 或敏感参数。
- [x] 公开接口只返回 `published` 内容。
- [x] 视觉令牌只写在 `frontend/src/index.css`，品牌文案只写在 `frontend/src/lib/brand.ts`。

## 2. 规模包线与退化策略

媒体数量 `N` 不确定，因此验收按规模包线定义。当前样本只用于校准，不代表上限。

| 包线 | N 范围 | 期望形态 | 最低验收 |
| --- | --- | --- | --- |
| P0 | `N <= 100` | 当前单实例可直接服务，手动预热足够。 | 首页无横向溢出；无 backend crash；派生失败有可见重试。 |
| P1 | `100 < N <= 1k` | 列表分页稳定，预热可分批执行，后台不再返回无界列表。 | 公开列表 p95 < 250ms；预热 1 条到 1k 均可中断/恢复；5xx < 0.5%。 |
| P2 | `1k < N <= 10k` | 管理页与首页不加载全量对象；预热、扫描和清理都有进度与限流。 | 公开列表 p95 < 400ms；扫描不重复删除索引；队列拒绝率 < 1%。 |
| P3 | `N > 10k` | 需要外部任务队列、更细的缓存策略或只读副本；不允许继续靠单进程线程池硬扛。 | 独立容量评审通过；有明确分片、队列、缓存和滚动重建方案。 |

增长不变量：

- [ ] 首页请求体量与首屏资源数保持常数，不随 `N` 线性增长。
- [ ] backend 内存峰值主要由并发任务数决定，而不是数据库总条数 `N`。
- [ ] 管理接口不得一次返回全量 `N` 条记忆；列表、导出和清理都必须有分页、批次或显式确认。
- [ ] 扫描必须支持“总数未知”，能断点恢复，且达到上限时明确报告 `limit_reached`，不把截断当成功。
- [ ] 预热批次默认不超过 `min(N, 200)`，不得因为一次预热占满公开请求槽位。

## 3. 当前基线

以下数据来自 2026-09-07 本地 PostgreSQL 和运行日志，是下一轮实验的输入。每次迭代开始应刷新；不能直接假设它们仍然准确。

| 指标 | 值 | 说明 |
| --- | ---: | --- |
| 当前记忆样本 | 65 | 只代表当前本地索引，不代表百度网盘总数。 |
| 照片 / 视频 | 48 / 17 | 视频占比约 26%。 |
| 远程索引文件 | 65 | `baidupan` 来源。 |
| 索引总大小 | 5.45 GiB | 最大单文件约 0.94 GiB。 |
| 派生状态 | 26 ready / 39 failed | 2026-09-07 受控复现成功 1 条；39 条仍待复现分类。 |
| backend 内存 | 256m 硬上限 | 2026-09-07 受控派生后 cgroup peak 207.99m，未 OOM 但超过 200m 安全目标。 |
| 派生并发 / 队列 | 1 / 32 | 保护 256m 上限；不能因 429 直接提高并发。 |
| 前端图像并发 | 2 | 首页可见卡片排队请求 240px，查看器高优先级。 |
| 已知格式风险 | MOV | 16 MiB 截断会缺 `moov atom`；视频首帧需要更大的临时源或专用探针。 |
| 存储审计 | 通过 | 2026-09-07：`photos/videos/tmp=0`，`thumbnails=58`，Source Zero 启动审计 `compliant=true`。 |

## 4. 观测与诊断

没有观测的优化不可验收。

- [ ] 建立一轮标准快照命令或脚本，输出：`N`、photo/video 数、published 数、索引大小、分位数大小、thumbnail ready/failed、scan 状态、preheat 状态。
- [ ] 后台状态页展示“最近 1 小时 / 24 小时”错误率，而不只展示累计计数。
- [ ] 区分 429 原因：队列满、等待超时、Nginx 限频、百度限频；不能全部显示为“请求过多”。
- [ ] 记录派生失败原因类型：远程 403/404、`moov` 缺失、编解码不支持、超时、backend 重启、磁盘配额不足。
- [ ] 记录前端体验指标：首屏可见缩略图成功率、加载耗时、排队等待、取消率、错误重试率、CLS。
- [ ] 采集 backend RSS/PSS、cgroup memory peak、OOM 事件、FFmpeg 子进程峰值、临时文件数量和磁盘配额。
- [ ] 日志能回答“哪个媒体 ID / 哪种尺寸 / 哪类格式失败”，但不得输出直链和凭证。

验收：任何性能改动合并前，至少能对比改动前后的错误率、资源峰值和首屏体验指标；没有对比数据只能标记为实验。

## 5. 数据与扫描迭代

目标：在 `N` 未知时保持索引可信、可恢复、可诊断。

- [ ] 新增资源盘点摘要，避免管理员只能靠记忆和数据库 SQL 判断媒体规模。
- [ ] 扫描结果明确展示“发现、更新、跳过、缺失、删除、上限触达”，并区分“未扫完”和“已完成”。
- [ ] `BAIDU_SCAN_MAX_ITEMS` 触顶时，管理页必须显示续扫入口；不得自动误删未扫到的索引。
- [ ] 删除缺失索引前必须有 dry-run、批次上限、失败恢复和操作日志。
- [ ] 数据库查询按 `N` 增长保持有界：公开列表、推荐、管理列表都不做无界全量加载。
- [ ] 管理列表补充分页 / 过滤 / 批量批次上限，替代当前可能的全量返回。
- [ ] 为同一远程文件建立稳定身份：`fs_id + remote_path` 为主，MD5 去重逻辑需要处理改名、移动和重复上传。

验收：在 1k 样本或合成样本上连续扫描、中断、续扫，索引数量和状态最终一致；没有重复记忆，也没有把未扫到的资源误删。

## 6. 派生图与预热迭代

目标：让首访用户看到可用缩略图，同时不让冷缓存请求压垮 256m backend。

- [ ] 把预热从“手动一次性任务”演进为可中断、可恢复、可按优先级调度的任务。
- [ ] 预热顺序使用列表顺序：先首页可见资源，再推荐资源，再后续分页；不是按数据库默认顺序盲扫。
- [ ] 失败项单独进入重试队列，并与“格式不支持”分开展示；不无限重试同一失败。
- [ ] 每个失败派生保留最小诊断：格式、尺寸、失败类型、时间、任务 ID。
- [ ] 派生只做单帧等比 WebP；输出尺寸矩阵继续按视口需要，不默认生成全部尺寸。
- [ ] 继续用 `memory_id + derivative_version + size` 合并相同请求；请求合并必须可被测试证明。
- [ ] 为 240 / 480 / 768 / 1280 建立命中率与生成成本看板，判断哪些尺寸值得保留。
- [ ] 实验“生成 240 后由小图放大作为临时降级”是否可接受；若画质不可接受，则只作为兜底并在 UI 标记降级。
- [ ] 不回退百度方形缩略图；任何降级都必须保持原始宽高比且不暴露远程地址。

验收：

- [ ] 冷缓存首页在 2 个前端并发、后端派生并发 1 的条件下无 backend crash。
- [ ] 429 不阻塞页面骨架和后续滚动；前端有退避重试，用户不看到连续失败。
- [ ] 对 40 个失败样本能分类出至少“可重试”和“需人工/格式限制”两类，并有重试成功率。
- [ ] 预热公开请求优先级高于低优先级批量任务；预热可被取消，且取消后不残留临时文件。

## 7. 前端体验迭代

目标：把“资源很多、网络不稳定、缩略图生成慢”变成可理解的渐进体验，而不是一次性等待。

- [ ] 首页只加载当前视口附近的资源；滚动追加时不回退、不跳动、不重复加载。
- [ ] 每张卡片在派生图加载前后都保持稳定占位，CLS < 0.05。
- [ ] 429 / 502 / timeout 使用统一退避重试；达到上限后显示轻量重试卡，不触发浏览器原始坏图。
- [ ] 查看器打开时优先复用卡片已有 240px 图，再排队加载更高清派生图。
- [ ] 视频播放仍按需获取短时直链；切换媒体或关闭查看器取消未完成图像/直链请求并释放播放器。
- [ ] 支持轻量筛选与定位，但当 `N` 很大时不把全部标题、标签或日期一次性载入前端。
- [ ] 空状态、扫描中、派生失败、网盘未授权、限流中分别有明确文案和下一步动作。
- [ ] 移动端触控、键盘导航、焦点可见、深浅主题和 `prefers-reduced-motion` 保持可用。

验收：

- [ ] Playwright 覆盖首屏只请求必要尺寸、无播放前视频请求、切换取消排队请求、错误后可重试。
- [ ] 真实样本下 1280 / 768 / 375 视口无横向溢出、无文字重叠、无关键控件小于 44px。
- [ ] 用户在派生失败时仍能理解当前状态并继续浏览其他资源，而不是整页不可用。

## 8. 可行方向探索

以下方向按当前证据排序。每项都要先做最小实验，再决定是否合入。

| 方向 | 假设 | 最小实验 | 采纳条件 | 拒绝/回滚条件 |
| --- | --- | --- | --- | --- |
| 预热规划器 | 按首页、推荐和失败优先级预热，比全量顺序预热更能降低首屏失败。 | 对当前 65 条生成预热计划，只处理失败和首页资源。 | 首屏缩略图成功率提升，backend 无 OOM。 | 5xx 或 OOM 增加；预热仍占用公开槽位。 |
| 失败分类器 | 429、502、MOV `moov` 缺失和编码不支持需要不同处理。 | 把派生失败原因写入状态/日志，并在后台分组展示。 | 502 不再被误判为普通可重试失败。 | 分类带来的写入/展示成本超过收益。 |
| 管理分页 | 管理页全量返回会在 `N` 增长后拖慢后台。 | 加分页和过滤，先只服务管理页。 | N=1k 模拟下管理页响应稳定。 | 操作流程明显变慢且无法接受。 |
| 前端虚拟化 | 首页只保留视口附近卡片可降低长列表内存。 | 用当前 24 条分段加载作为基线，再模拟 1k 条。 | DOM 数量和内存下降且滚动不跳动。 | 动效、滚动恢复或无障碍退化。 |
| 派生降级 | 低尺寸图临时放大可避免用户看到失败。 | 对已存在 240 的资源尝试放大到 480/768，并做视觉验收。 | 用户可接受且失败率显著下降。 | 画质明显劣化或用户无法知道当前是降级。 |
| 独立 worker | FFmpeg/预热与 API 进程分离能保护 256m API 容器。 | 用同一镜像启动 worker 进程，共享数据库，不暴露端口。 | API 容器内存峰值为 0 OOM，任务吞吐提升。 | 增加部署复杂度但错误率无下降。 |
| 外部队列 | Redis 队列或任务系统适合 P2 以上规模。 | 先设计 API/表结构，不直接替换当前进程内队列。 | P1/P2 实验证明队列恢复、取消和重试更可靠。 | 单实例场景收益不足以抵消运维复杂度。 |
| 派生缓存 CDN | 公开 immutable 派生图可以卸载重复请求。 | 只对公开 240/480 图评估缓存头与命中率，不缓存私有数据。 | 后端命中率下降，错误率下降。 | 可能缓存私有/带凭证响应。 |
| 日期/地点分组 | 按时间或地点浏览比无限瀑布更适合大量回忆。 | 在 240px 视口原型分组列表，不做后端大改动。 | 用户能更快定位内容。 | 增加 payload 或影响瀑布流稳定性。 |

## 9. 验收门禁

### 产品门禁

- [ ] 用户能理解当前资源数量、扫描状态、失败状态和下一步动作。
- [ ] 用户无需等待全部派生图完成即可浏览已就绪内容。
- [ ] 失败只影响单张卡片，不导致整页不可用。

### 性能门禁

- [ ] 公开列表 p95 不随 `N` 线性恶化。
- [ ] 首屏请求数量、图像请求数量和传输量保持常数或下降。
- [ ] CLS < 0.05；交互后不得出现明显重叠、滚动跳动或误触。
- [ ] 429 / 5xx 错误率下降，且用户可感知的重试成功率提升。

### 资源门禁

- [ ] backend cgroup memory peak < 200m，留出 256m 的安全余量。
- [ ] OOM / OOM kill = 0。
- [ ] FFmpeg 子进程无堆积；`media/tmp` 在成功、失败、取消和重启后为空。
- [ ] 磁盘派生缓存和 Nginx 缓存在配额内，清理不触碰网盘资源。
- [ ] Source Zero：DB 中 `baidupan` 记录的 `source_path` 为零；服务端无照片/视频/音频源文件；`media/photos` 目录为空或不存在。

### 可靠性门禁

- [ ] 所有后台任务可中断、可恢复、有幂等键。
- [ ] 重复请求不会重复派生；重复扫描不会重复插入或误删。
- [ ] 服务重启后用户状态、扫描进度和派生状态可以解释，不需要人工猜数据库。

### 工程门禁

- [ ] 后端 pytest 全量通过。
- [ ] 前端 typecheck 和 production build 通过。
- [ ] Playwright 冒烟、体验、并发和错误路径用例通过。
- [ ] 设计令牌、品牌文案、whitespace、Compose 配置和 `git diff --check` 通过。
- [ ] 本地任务分支保留合并提交合回 `main`。

### 真实容器门禁

- [ ] `docker compose ps` 显示 backend / db / redis / nginx 全部 healthy，且只有 Nginx 暴露宿主端口。
- [ ] 通过真实 Nginx 请求 `/healthz`、公开列表、详情、缩略图和播放边界；不使用 route mock 代替服务端。
- [ ] 缩略图响应为 `image/webp`，直链响应为 `no-store`；验证后不得把直链写入 DB、日志或前端存储。
- [ ] 真实浏览器模拟确认首页渲染、卡片可见、查看器打开、无播放前视频本体请求。

## 10. 迭代 Ledger

Ledger 只追加，不重写历史。每轮的结论会改变下一轮优先级。

| ID | 问题 / 假设 | 实验 | 验收口径 | 结果 | 决策 |
| --- | --- | --- | --- | --- | --- |
| I0 | 当前体感是否可靠？ | 采样本地 DB 数量、派生状态和近期运行日志。 | 输出基线值、已知失败类型和规模包线。 | 2026-09-07：65 条、5.45 GiB、25 ready / 40 failed；MOV 与 backend OOM 风险已识别。 | 进入派生失败分类与预热规划。 |
| I1 | 失败能否按“可重试 / 格式限制 / 临时错误”分组？ | 对 40 个 failed 派生记录失败原因并做一次受控重试。 | 每个失败有类型；可重试项成功率可量化。 | 待实验。 | 待定 |
| I2 | 首页优先预热能否降低用户可见失败？ | 只预热首页、推荐和失败重试资源，对比冷缓存错误率。 | 首屏失败率和 429/5xx 下降，backend 无 OOM。 | 待实验。 | 待定 |
| I3 | 管理页在 N 增长时是否稳定？ | 模拟 1k 记录，测量当前管理列表接口和页面渲染。 | 响应与 DOM 不随 N 无界增长。 | 待实验。 | 待定 |
| I4 | 低尺寸降级是否可以接受？ | 用已存在 240px 派生图临时服务更高请求，并做视觉验收。 | 失败卡减少且画质可接受。 | 待实验。 | 待定 |
| I5 | 服务端是否残留源资源？ | 审计 DB `source_path`、`media/photos`、源媒体扩展名和 BLOB。 | Source Zero = 2 分；无 PNG/JPG/MOV/MP4 源文件。 | 2026-09-07：DB 65 条 `baidupan` 且 `source_path=0`；但 `media/photos` 发现 3 PNG + 1 ICO 历史遗留。 | 清理遗留文件，并把 Source Zero 审计加入每轮闭环。 |
| I6 | 清理后能否由启动审计和测试门禁持续证明 Source Zero？ | 确认 65 条记录 `source_path=0`；删除 4 个历史遗留文件；新增 DB/文件/Pydantic/API 审计并接入启动日志与 pytest；重建 backend 后走真实 Nginx 链路。 | `photos/videos/tmp=0`、源媒体文件与 BLOB 为 0、DB `source_path=0`；四容器 healthy；真实浏览器无播放前视频请求。 | 2026-09-07：`source_zero.compliant=true`；文件计数 `photos=0,videos=0,tmp=0,thumbnails=58`；cgroup peak 129.8m，OOM=0；健康/列表/详情/240 WebP/直链 `no-store` 通过；真实 Chrome 打开视频查看器且 `direct-url`、`file` 请求均为 0。 | 采纳；G0 当前轮=2。后续快照必须携带 `source_zero`，下一实验转向 I1 失败分类。 |
| I7 | 40 条派生失败能否从“统一 failed”变成可归因分类？ | 新增 `thumbnail_failure_kind` 迁移、异常分类器、失败 sink、状态分组与后台文案；迁移 0008 后重建真实容器，对最小失败 JPEG 做 1 条受控复现。 | 新失败必须落库分类；后台按类分组；受控重试可成功且有资源指标；不输出直链和凭证。 | 2026-09-07：迁移通过；65 个 pytest、24 个 Playwright、真实 Nginx/Chrome 播放边界通过；受控 1.4MB JPEG 生成 240px，`tmp=0`，ready 26 / failed 39，39 条历史失败仍 `unclassified`；cgroup peak 207.99m，OOM=0。 | 采纳分类器；因峰值超过 200m，不做批量重试，继续观察资源并优先设计低内存复现/独立 worker。 |

## 11. 当前优先级

1. 先完成失败派生分类，把 429、502、MOV `moov`、编码限制和临时错误分开。
2. 再实现首页优先的可恢复预热，验证能否把首屏失败率降到可接受区间。
3. 同步补齐管理页分页与资源盘点摘要，为 `N` 超过当前 65 条做准备。
4. 只有当以上数据证明需要跨进程治理时，才迁移到独立 worker 或外部队列。
5. 每轮快照继续携带 Source Zero 审计；若出现新的源文件或 `source_path`，先回到本项再继续性能实验。
