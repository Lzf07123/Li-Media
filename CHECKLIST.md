# Li&Media 首页加载体验、预热提示与资源边界 Checklist

> 更新于 2026-09-08。本清单取代“后台媒体盘点、视频兼容筛选与首页呈现 Checklist”。
> C1–C4 功能与自动化验收已实施；C3/C4 的真实容器复验仍待执行。
> 目标是吸收成熟媒体产品的首页加载做法，公开预热状态，补齐后台磁盘占用观测，并把关键资源限制改为可配置。

## 0. 范围与边界

- [ ] 只处理 `photo` 与 `video`，不新增内容类型。
- [ ] 只调整首页加载与状态提示、后台状态指标、资源限制配置和必要接口；不修改百度网盘索引、播放、转码或删除链路。
- [ ] 首页提示只公开运维状态摘要，不暴露管理令牌、远端路径、真实凭证、失败堆栈或内部队列明细。
- [ ] 预热状态必须基于服务端权威数据；前端不得用本地图片成功数量推断后台预热状态。
- [ ] 磁盘占用指标只读取容器可见的本地目录与文件系统用量，不访问百度网盘配额。
- [ ] 资源限制可配置后仍保留安全默认值、上下限和错误提示；不能因为配置错误绕过容器硬限制。
- [ ] 视觉令牌仍只写在 `frontend/src/index.css`；品牌与界面文案仍只写在 `frontend/src/lib/brand.ts`。
- [ ] 动效必须尊重 `prefers-reduced-motion`，不产生布局跳动、大面积重色或常驻装饰。
- [ ] 公开数据仍只返回 `published` 且可展示的资源，不泄露草稿、下线、排除或不可播放内容的存在。

## 1. 首页加载体验参考与适配

### 需求

- [ ] 参考 Unsplash、Apple Photos、Google Photos、Pinterest、小红书等同类产品的首页做法，比较以下能力后再落地：渐进式图片、低质量占位、稳定瀑布流、可见优先加载、优先级提示、骨架屏、失败重试和缓存回放。
- [ ] 首屏优先加载视口内资源；非可见资源懒加载，滚动接近视口时再排队。
- [ ] 每张卡片在请求前、加载中、加载完成和加载失败之间使用连续状态，不出现空白块或整卡抖动。
- [ ] 占位必须使用媒体真实宽高或已知比例；媒体返回后卡片宽高、列位和相邻卡片不移动。
- [ ] 图片加载使用响应式尺寸与 `sizes` / `srcSet` 能力；首屏关键图可使用 `fetchPriority="high"`，其余卡片保持低优先级。
- [ ] 保留并复核图片请求队列：可见卡片优先、查看器高优先级、切换或卸载时中止无效请求。
- [ ] 缓存命中或响应很快时不显示短暂闪烁；慢响应超过阈值后才展示掠光或骨架反馈。
- [ ] 掠光只作为等待反馈，不遮挡媒体；加载完成后立即移除，失败态切换为单卡轻量重试。
- [ ] 动效优先使用 `transform` / `opacity` / `background-position` 等合成属性；不逐帧修改布局属性。
- [ ] 滚动追加、类型筛选、查看器关闭和返回首页时保持滚动位置；不重复请求或清空画布。
- [ ] 429、5xx、timeout 只影响受影响的卡片或当前分段；提供明确重试入口，不显示整页“回忆库暂不可用”。
- [ ] 新提示、进度条和状态徽标必须有可读标签、键盘可聚焦区域和读屏友好语义；正文对比度达到 AA。

### 验收

- [ ] 用延迟响应验证占位、掠光、完成态和失败态；用缓存或即时响应验证没有短暂闪烁。
- [ ] 断言加载完成后掠光层不存在；`prefers-reduced-motion` 下不播放掠光和入场动画。
- [ ] Playwright 覆盖首屏优先、懒加载、滚动追加、查看器返回、失败重试和队列取消。
- [ ] 375 / 768 / 1280 / 1600 视口无横向溢出、无卡片重叠、无文本遮挡。
- [ ] CLS < 0.05；同一卡片加载前后宽高一致，相邻卡片不移位。
- [ ] 记录改动前后首屏成功率、首图耗时、重试率和长任务主线程阻塞证据。

## 2. 首页预热状态显式提示

### 需求

- [ ] 提供公开只读接口或公开计数附带状态，返回安全的预热摘要：`running`、`not_preheated`、`ready`、`degraded` 或等价状态。
- [ ] 后台预热任务处于 `queued` 或 `running` 时，首页显示明确提示，例如“后台正在预热预览”。
- [ ] 没有预热记录且存在可公开浏览资源时，首页显示“预览缓存未预热，首次加载可能较慢”。
- [ ] 预热完成且缓存命中率或候选进度达到定义口径时，提示可收敛为“预览已就绪”，不常驻打扰。
- [ ] 预热取消或失败时显示“预热未完成，首次加载可能较慢”，但不显示失败原因、资源 ID 或内部错误。
- [ ] 提示包含可理解的进度口径，如已处理 / 总数；不包含总库、失败明细或非公开资源数量。
- [ ] 接口不可用或返回异常时，前端显示“预热状态不可用”，不误报成功或空状态。
- [ ] 提示状态可被读屏识别；更新时使用 `aria-live="polite"`，避免每次轮询打断用户。
- [ ] 预热任务结束后提示自动收敛；下一次加载或刷新可以读取最新状态。
- [ ] 首页不保存长期预热状态；不把后台任务 ID、内部队列容量或调度线程数暴露给访客。

### 验收

- [ ] 后端测试覆盖无任务、排队、执行中、完成、取消、失败和空库七种状态。
- [ ] 后端测试确认响应没有 `admin_token`、`client_secret`、完整远端路径、资源 ID、失败堆栈和内部任务 ID。
- [ ] 前端测试断言执行中与未预热提示可见，完成后自动收敛；接口失败显示独立不可用状态。
- [ ] Playwright 验证提示不遮挡筛选条、标题、页脚和媒体卡片；375 视口无横向溢出。
- [ ] 读屏语义检查通过：进度、状态和恢复提示能被 `aria-live` 或等价机制感知。

## 3. 后台磁盘占用指标

### 需求

- [ ] `/admin/system/status` 新增磁盘占用指标，至少包含文件系统总量、已用、可用和占用百分比。
- [ ] 单独展示派生图缓存、临时文件和 Nginx 缓存的文件数与字节数，便于判断占用来源。
- [ ] 指标路径来自服务端配置；响应不得返回主机绝对路径、挂载配置或越权目录内容。
- [ ] 数据库、Redis 或远端网盘不可用时，本地磁盘指标仍能正常采集并标记可用状态。
- [ ] 目录遍历有超时或数量边界；文件系统扫描失败返回明确 `unavailable`，不阻塞后台页面。
- [ ] 百分比达到 80% 显示警示；达到 90% 显示危险态，颜色使用语义令牌。
- [ ] 后台资源面板显示 `已用 / 总量` 和主要缓存细分；格式化后的文本不换行溢出、不遮挡相邻指标。
- [ ] 快照命令 `system_snapshot.py` 继续输出资源数据，并包含新增磁盘指标，便于部署前后对比。
- [ ] 空目录返回 0 而不是 `null`；路径不可读时指标明确标记不可用，不显示误导性的 0。

### 验收

- [ ] 后端测试覆盖派生图、临时文件、Nginx 缓存、空目录、不可读路径和文件系统统计。
- [ ] 后端测试确认磁盘数据不在公开接口中出现，仅出现在管理员鉴权后的系统状态接口。
- [ ] 前端测试断言进度条、数字、缓存细分和阈值状态渲染正确。
- [ ] 真实容器验证 `/app/data`、Nginx 缓存与派生图缓存的合计结果可解释；指标刷新后数值稳定。
- [ ] 快照记录文件系统占用与各类缓存字节数，无 OOM、无临时文件残留。

## 4. 资源限制可配置

### 需求

- [ ] 将关键资源限制迁移到环境变量并保留现有默认值，至少覆盖：预热并发、预热队列、单帧派生并发、派生队列、直链探测并发、扫描并发、流回退全局并发、单用户流并发、临时文件数量和临时磁盘配额。
- [ ] 每项配置有明确上下限、类型校验和单位；非法值不能静默使用另一个值，应在启动或配置读取处失败。
- [ ] `0` 只用于明确允许“不限制”或“关闭”的语义；不允许误解为无限队列或无限并发。
- [ ] 配置调整不得突破容器 `mem_limit`、`pids_limit` 和磁盘硬边界；文档说明实际峰值可能高于单任务理论值。
- [ ] `.env.example` 记录默认值、推荐范围和风险说明；不记录真实主机路径、密钥或私有地址。
- [ ] 配置变更写入 README 或部署说明，说明是否需要重启、是否影响 Nginx 缓存、是否需要重新预热。
- [ ] 后台状态面板显示生效值，方便确认运行时配置而非假设配置。
- [ ] 预热、派生、扫描和流回退仍共用现有优先级与全局限流；公开请求优先级不能被预热配置压低。
- [ ] 修改配置后任务队列、拒绝计数、活跃任务、临时文件和磁盘指标仍能正确反映。
- [ ] 不提供运行中热更新；配置只在服务启动时加载，避免不同 worker 使用不一致阈值。

### 验收

- [ ] 配置测试覆盖默认值、最小值、最大值、非法值、边界值和显式 `0` 的语义。
- [ ] 后端测试确认各任务治理器读取配置后的 `limit`、`queue_limit` 和超时值一致。
- [ ] 集成测试验证公开请求优先级仍高于预热；配置扩大预热并发后公开列表仍不受抢占。
- [ ] Compose 配置测试验证环境变量能传入 backend，默认配置与 `.env.example` 保持一致。
- [ ] 真实容器重启后后台状态显示新值；旧队列中的任务在升级前已结束或可安全恢复。
- [ ] 压测或受控基线记录峰值内存、进程数、临时文件和磁盘占用；无 OOM、无临时文件残留。

## 5. 工程门禁

- [ ] 后端 pytest 全量通过。
- [ ] 前端 typecheck 和 production build 通过。
- [ ] Playwright 覆盖首页加载反馈、预热提示、后台磁盘指标和配置生效值。
- [ ] 视觉令牌仍只在 `frontend/src/index.css`；品牌与界面文案仍只写在 `frontend/src/lib/brand.ts`。
- [ ] 正文对比度达到 AA；可点击目标不小于 44×44px。
- [ ] `docker compose config`、whitespace / `git diff --check` 和设计来源检查通过。
- [ ] 真实 Nginx 链路通过，backend / db / redis / nginx 全部 healthy。
- [ ] backend 内存上限保持 256m；本轮不引入源媒体下载、完整转码或本地上传。

## 6. 交付顺序

1. 先锁定预热状态公开契约和磁盘占用指标 schema，补齐接口测试。
2. 再实现首页提示与加载体验，不把状态契约和视觉调整混在一个回滚单元。
3. 再接入后台资源面板的磁盘占用与缓存细分。
4. 最后扩展资源限制配置，并复核默认值、上下限、文档和部署行为。
5. 每一步先过自动化门禁，再做真实容器验收；加载体验改动需有改动前后指标。

## 7. Ledger

| ID | 事项 | 状态 | 结论 |
| --- | --- | --- | --- |
| C0 | 清单定版 | 2026-09-08 完成 | 定版首页加载参考、预热状态提示、后台磁盘占用指标和可配置资源限制；未实施功能。 |
| C1 | 首页加载体验参考与适配 | 2026-09-08 完成 | 补齐响应式 `srcSet`、低优先级非关键图、安全预热提示与既有稳定占位/队列/重试闭环；Playwright 覆盖 375/768/1280/1600、CLS < 0.05 和加载状态。 |
| C2 | 首页预热状态显式提示 | 2026-09-08 完成 | 新增公开 `/memories/preheat-status` 安全摘要，覆盖排队/执行中、未预热、完成、取消/失败、空库和接口失败；前端使用 `aria-live` 并自动收敛。 |
| C3 | 后台磁盘占用指标 | 2026-09-08 自动化完成 | 管理状态返回文件系统用量与派生图/临时/Nginx 缓存细分，含阈值、边界和不可用语义；快照同步输出。真实容器复验待执行。 |
| C4 | 资源限制可配置 | 2026-09-08 自动化完成 | 全量关键限制接入环境变量与边界校验，后台显示生效值，文档说明重启与峰值风险；真实容器重启和压测复验待执行。 |
