# Li&Media 后台媒体盘点、视频兼容筛选与首页加载显示 Checklist

> 更新于 2026-09-07。本清单取代“自适应媒体体验 Checklist”。
> 本轮只定版范围与验收，不实施功能改动。
> 目标是让管理员看清媒体规模、按筛选范围批量发布或下线、保证筛选与计数一致；公开媒体自动筛除浏览器不可播放的视频；同时增强首页资源加载表现，例如卡片掠光。

## 0. 范围与边界

- [ ] 只处理 `photo` 与 `video`，不新增内容类型。
- [ ] 后台部分只调整管理接口、后台页面和必要的数据查询；不改 Source Zero、网盘扫描、派生生成和播放链路。
- [ ] 首页部分只增强加载中的视觉反馈与状态切换，不改后端并发上限、派生算法、视频转码或播放传输链路。
- [ ] 发布/下线只修改管理状态，不修改或删除百度网盘资源。
- [ ] 公开展示仍受展示健康规则约束：`published` 不等于一定 `displayable`。
- [ ] 公开视频展示必须叠加浏览器兼容筛选：已发布但浏览器不可播放的视频不进入公开画布，也不进入公开推荐。
- [ ] 视频兼容自动筛选只调整展示状态和公开可见性，不删除索引、不修改百度网盘资源、不做完整转码。
- [ ] 所有计数以后端查询为唯一权威；前端不得用当前页 `items.length` 推导总数。
- [ ] 批量操作必须有明确范围、确认入口、结果反馈、审计记录和失败恢复语义。
- [ ] 卡片掠光是加载状态反馈，不是常驻装饰；动画必须可禁用、可回收，且不影响真实媒体可见性。

## 1. 输入基线

以下数据来自 2026-09-07 真实容器，只作为测试参照，不是完成证明：

| 口径 | 数量 |
| --- | ---: |
| 后台索引总数 | 65 |
| 后台照片 / 视频 | 48 / 17 |
| 展示健康可展示 / 排除 | 64 / 1 |
| 公开列表可展示总数 | 64 |
| 公开列表照片 / 视频 | 47 / 17 |

## 2. 后台媒体总数与分类计数

### 需求

- [ ] 后台顶部或列表工具栏显示索引总数、照片数和视频数。
- [ ] 计数必须覆盖后台索引全量，不受当前页大小或分页截断影响。
- [ ] 类型计数显示为 `总数 / 照片 / 视频`，并满足 `照片数 + 视频数 = 总数`。
- [ ] 状态筛选、展示健康筛选和关键词筛选生效时，结果数必须随之变化，但类型计数应能清楚区分“全局索引计数”与“当前筛选命中数”。
- [ ] 若同时展示 `published`、`draft`、`displayable`、`excluded` 计数，标签必须写明口径，不能把发布状态与展示健康混为一谈。
- [ ] 空库、零视频、零照片、接口失败和加载中分别有明确状态，不显示误导性的 0。

### 验收

- [ ] 后端集成测试覆盖照片、视频、published、unpublished、displayable、excluded 的组合计数。
- [ ] 任意筛选组合下，接口返回的 `total`、类型计数、状态计数与 SQL 查询结果一致。
- [ ] 前端真实容器测试对比 API 响应与页面文本，确认计数一致。
- [ ] 修改分页大小、翻页、切换筛选后，全局类型计数保持稳定。

## 3. 一键发布与一键下线

### 需求

- [ ] 后台提供“一键发布当前筛选”和“一键下线当前筛选”两个明确操作。
- [ ] 默认作用域是当前筛选命中的全部记录，而不是当前页。
- [ ] 操作前弹出确认框，展示筛选条件、影响总数、照片/视频分布、发布/下线状态分布和展示健康分布。
- [ ] “全部媒体”属于高风险作用域；若没有筛选条件，确认文案必须显式提示将影响全库。
- [ ] 后端按同一筛选条件执行，不依赖前端上传全量 ID；超过 100 条时使用后台任务或分批处理。
- [ ] 批量任务必须显示处理进度、成功数、跳过数、失败数和可重试原因；支持取消，且取消后已完成状态保留。
- [ ] 重复执行是幂等的：已发布资源再发布、已下线资源再下线应标记 skipped，不产生误导性失败。
- [ ] 操作完成后，列表、筛选计数、状态计数和展示健康计数统一刷新。
- [ ] 操作日志记录动作、筛选快照、影响数量、成功/跳过/失败数量、管理员会话和资源 ID；不得记录完整直链、Token 或 Cookie。
- [ ] 用户权限不足、会话过期、接口超时或部分失败时，前端显示明确下一步动作，不得误报全部成功。

### 验收

- [ ] 集成测试覆盖“全部、仅照片、仅视频、关键词筛选、展示健康筛选”五种作用域的发布与下线。
- [ ] 测试确认只改变指定范围内记录的状态，范围外记录不变。
- [ ] 测试确认重复调用返回幂等结果，且操作日志不重复记录误导性变更。
- [ ] 测试确认 `excluded` 记录可以被发布或下线，但公开接口仍按展示健康规则剔除。
- [ ] 真实容器验证确认进度、结果摘要、刷新后的计数和公开列表行为一致。

## 4. 筛选显示正确性

### 需求

- [ ] 类型筛选、状态筛选、展示健康筛选和关键词筛选使用同一套后端谓词生成列表与计数。
- [ ] `全部` 计数等于照片数加视频数；`照片` 与 `视频` 计数互斥且不重叠。
- [ ] 当前选中筛选必须有清晰的视觉状态和可访问名称；键盘与读屏可识别。
- [ ] 筛选条、结果总数、分页总页数和列表行数相互一致。
- [ ] 筛选后没有结果时显示空态和清除筛选入口，而不是空白页面或旧数据。
- [ ] 加载、刷新和批量操作过程中防止重复提交；过期响应不得覆盖较新的结果。
- [ ] 批量发布/下线后仍保留当前筛选，不允许用户被重置回无筛选列表。

### 验收

- [ ] 后端测试用固定种子数据验证所有筛选组合的行集与计数。
- [ ] 前端测试验证筛选按钮数量、当前选中态、结果数量、分页和空态。
- [ ] 真实容器测试对比当前基线：后台索引 65 / 48 / 17，展示健康 64 / 1，公开列表 64 / 47 / 17。
- [ ] 批量操作后重新读取计数，确认页面没有显示旧缓存数据。

## 5. 视频浏览器兼容自动筛选

### 需求

- [ ] 为视频建立浏览器兼容展示状态，至少区分 `supported`、`unsupported` 和 `unknown`；状态必须持久化，避免每次请求重复探测。
- [ ] 公开列表、推荐和详情对视频统一叠加浏览器兼容谓词：只有 `supported` 视频可展示；`unsupported` 与 `unknown` 默认从公开画布剔除。
- [ ] 照片不受视频兼容筛选影响；后台索引中的视频总数和远程文件仍保持完整。
- [ ] 兼容判定使用保守格式矩阵，优先依据已持久化的容器、编码、profile 和音频编码；没有充分证据时不把视频标记为 `supported`。
- [ ] 需要补充探测时使用已有元数据或受控短程探测，不得下载完整源媒体、不得完整转码、不得引入全量扫描。
- [ ] 探测结果保存状态、格式摘要、失败原因、探测时间和版本；不保存源媒体副本、完整直链、Token 或 Cookie。
- [ ] 管理页支持按 `supported / unsupported / unknown` 筛选，并展示视频总数、浏览器可播放数、不可播放数和未知数。
- [ ] 管理员仍能看到不可播放视频及其原因；公开详情接口对不可播放视频返回明确的不可展示语义，不暴露远程地址。
- [ ] 展示健康、发布状态和浏览器兼容是三个独立口径；公开视频必须同时满足已发布、展示健康和浏览器可播放。
- [ ] 兼容状态变化后同步刷新计数、列表、详情和 Nginx 短缓存；不得让已剔除视频继续出现在缓存响应或推荐中。
- [ ] 浏览器兼容矩阵需要登记假设和来源；后续如果要支持 Safari HEVC 等浏览器特有能力，必须按客户端能力细化，而不是默认放宽全站谓词。

### 验收

- [ ] 固定种子数据覆盖：可播放 MP4、不可播放编码、未知编码、照片、未发布、展示健康排除和发布状态组合。
- [ ] 后端测试确认公开列表、推荐和详情使用同一兼容谓词；后台列表可按兼容状态筛选且计数正确。
- [ ] 测试确认兼容状态只影响公开可见性，不删除索引、不修改远程资源、不改变照片展示。
- [ ] 测试确认 `unknown` 默认不进入公开展示，且不会因刷新反复触发探测。
- [ ] 真实容器验证公开列表与推荐只包含浏览器可播放视频；照片数量和可播放视频数量与 API 计数一致。
- [ ] 真实浏览器验证可播放视频能进入查看器；不可播放视频不出现在画布、推荐和旧详情地址的公开展示中。
- [ ] 快照记录探测数量、状态分布、资源峰值、临时文件和 Source Zero 审计；无源媒体落盘、无完整转码、无 OOM。

## 6. 首页资源加载显示增强

### 需求

- [ ] 卡片在请求前、加载中、加载完成和加载失败四个阶段使用连续状态，不出现整块空白或布局跳动。
- [ ] 探索“卡片掠光 / shimmer”作为加载中反馈：掠光只出现在等待派生图的卡片上，加载完成后立即移除。
- [ ] 占位必须使用媒体真实宽高或已知比例驱动，保证媒体返回后卡片尺寸、列位和相邻卡片不跳动。
- [ ] 掠光不得替代图片：媒体可见后不保留遮罩、模糊层或动画残留；失败态切换为轻量重试卡。
- [ ] 掠光运动优先使用 `transform` / `opacity` 合成层动画，不逐帧改布局属性，不引入新的轮询或全屏监听。
- [ ] 掠光幅度、频率和对比度保持克制，不遮挡媒体轮廓，不形成大面积重色或霓虹光效。
- [ ] 用户开启 `prefers-reduced-motion` 时显示静态柔和占位，不播放掠光动画。
- [ ] 缓存命中或响应很快时不得出现闪烁；需要定义最小显示阈值，避免短暂加载被误造成动画抖动。
- [ ] 新卡片进入视口、筛选切换、滚动追加和查看器关闭返回时都使用同一加载语义；不会重复请求、清空画布或丢失滚动位置。
- [ ] 失败只影响单张卡片；429 / 5xx / timeout 显示可重试状态，不触发整页“回忆库暂不可用”。
- [ ] 首页图像排队策略保持有效：可见卡片优先、查看器高优先级、切换或取消时中止不再需要的请求。

### 验收

- [ ] Playwright 覆盖等待态、完成态、失败态、重试态和取消态；断言加载完成后掠光层不存在。
- [ ] 用延迟响应验证卡片有稳定占位和加载反馈；用缓存或即时响应验证没有短暂闪烁。
- [ ] 375 / 768 / 1280 / 1600 视口无横向溢出、无卡片重叠、无文字遮挡。
- [ ] CLS < 0.05；同一卡片加载前后宽高一致；相邻卡片不因媒体返回而移位。
- [ ] `prefers-reduced-motion` 下通过样式或运行时开关确认掠光动画不执行。
- [ ] 真实容器验证首页可见卡片只请求必要尺寸，刷新、滚动追加和查看器返回后的网络行为符合排队策略。
- [ ] 采集首屏可见卡片成功率、加载耗时、重试率和动画期间的主线程阻塞证据；改动前后必须有可比数据。

## 7. 工程门禁

- [ ] 后端 pytest 全量通过。
- [ ] 前端 typecheck 和 production build 通过。
- [ ] Playwright 覆盖计数、筛选、视频兼容剔除、确认弹窗、批量进度、幂等结果、失败态和首页加载显示。
- [ ] 视觉令牌仍只在 `frontend/src/index.css`；品牌与界面文案仍只在 `frontend/src/lib/brand.ts`。
- [ ] 正文对比度达到 AA；可点击目标不小于 44×44px。
- [ ] `docker compose config`、whitespace / `git diff --check` 和设计来源检查通过。
- [ ] 真实 Nginx 链路通过，backend / db / redis / nginx 全部 healthy。
- [ ] backend 内存上限保持 256m；批量操作不得引入源媒体下载或完整转码。

## 8. 交付顺序

1. 先锁定筛选谓词和计数契约，补齐接口测试。
2. 再持久化视频浏览器兼容状态，并把 `supported` 纳入公开视频谓词。
3. 再实现后台计数展示与筛选一致性。
4. 最后接入一键发布/下线、确认弹窗、任务进度和操作日志。
5. 首页加载显示可与后台计数并行设计，但必须独立提交、独立验收，不与批量操作混在同一回滚单元。
6. 每一步都先过自动化门禁，再做真实容器验收。

## 9. Ledger

| ID | 事项 | 状态 | 结论 |
| --- | --- | --- | --- |
| C0 | 清单定版 | 2026-09-07 完成 | 定版后台计数、批量发布/下线、筛选一致性、浏览器可播放视频自动筛选和首页加载显示增强；未实施功能。 |
| C1 | 后台媒体总数与分类计数 | 待实施 | 待定 |
| C2 | 一键发布与一键下线 | 待实施 | 待定 |
| C3 | 筛选显示正确性 | 待实施 | 待定 |
| C4 | 首页卡片掠光与加载状态增强 | 待实施 | 待定 |
| C5 | 浏览器可播放视频自动筛选 | 待实施 | 待定 |
